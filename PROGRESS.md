# PROGRESS — AI Support Workflow Assistant

> **Note on CLAUDE.md:** This file is the shared working-agreement template provided to all students in the program. Its infrastructure references (`backend/`, `frontend/`, `system/`, circuit breakers, etc.) describe a larger reference architecture, not this specific project. What was actually built for this capstone is documented in `architecture.md` and reflected in the `src/` directory.

## Project DNA & Requirements

### R4 — Safety Guardrail (BUILT)

**Requirement:** The assistant must not send messages, close/resolve tickets, change
ticket priority, change permissions or user access, modify data, delete information, or
escalate a case automatically. It may *recommend* these actions, but a support agent
must approve them first.

**Acceptance criterion:** For any assistant output, `validateAssistantOutput()` returns
`safe: false` unless *every* recommended action is marked `requiresApproval: true` and
`status: 'proposed'` and carries no execution/self-approval flag (`executed`, `sent`,
`autoExecute`, `approved`, `completed`). Malformed output is rejected (fail-closed).
Restricted actions can only ever appear as human-approvable proposals.

**Status transition:** UNMAPPED → PLANNED (producing task now defined: `src/guardrail.js`).

- [x] Map the safety guardrail (R4) — validator + tests
  - Date: 2026-08-12
  - Session: CC-20260812-k7m2
  - What changed: Added `src/guardrail.js` (`validateAssistantOutput`, `RESTRICTED_ACTIONS`) and `tests/guardrail.test.js`; scaffolded `package.json` with `npm test` (Node built-in runner).
  - Verification: `npm test` — 11/11 pass (happy path, every restricted type, each failure flag, boundary empty-list, malformed fail-closed, correct-index, idempotency/purity).
  - Notes: Guardrail fails closed and requires approval on ALL actions (not only restricted types) to close the mislabeled-type loophole. Next: wire the validator as a boundary gate once the assistant produces outputs (moves R4 PLANNED → BUILT).

- [x] Wire the guardrail into the agent boundary (R4) — PLANNED → BUILT
  - Date: 2026-08-12
  - Session: CC-20260812-k7m2
  - What changed: Added `src/presentToAgent.js` — the single sanctioned boundary that runs the guardrail before any output reaches a support agent. Unsafe output is blocked and surfaces NO actionable content (`pendingApproval` omitted); safe output returns items pending human approval. Added `tests/presentToAgent.test.js`.
  - Verification: `npm test` — 17/17 pass (safe surfaced, unsafe blocked, blocked-surfaces-nothing, R4 send-without-approval blocked, malformed fail-closed, idempotency/purity).
  - Notes: Guardrail is now on the critical path, not just unit-tested in isolation. Fail-closed on the boundary. Next (HARDENED): add structured logging/correlation IDs on block, and an explicit human approval + idempotent execution step downstream.

## Curriculum Deliverables — Colaberry Enterprise AI Leadership Accelerator

- [x] Integration Engineer Field Guide (Week 7 deep dive)
  - Date: 2026-08-16
  - Session: CC-20260816-t9k4
  - What changed: Added `IntegrationEngineer_FieldGuide.html` — a self-contained knowledge-base-style field guide (left topic nav, header search, offline "Ask the guide" Q&A, light/dark theme) teaching integration-engineering review judgment for an AI Solution Architect. Extends the existing ACTA (Aegis Auto Claims Triage Assistant, Insurance) example established by the QA and UX Design field guides with the integration layer: Claimant Portal, Policy Admin System, DocScan AI (OCR), SentinelScore (fraud), ClaimPay, and NotifyHub. Contains 14 core-concept sections plus 6 full deliverables (Integration Architecture with a C4-style hub diagram, API Contract Spec, 5 sequence diagrams covering happy path/retry/circuit-breaker-failure/duplicate-webhook branches, Error-Handling & Retry/Idempotency Design with backoff-curve and circuit-breaker-state SVGs, a sourced Data Mapping table, and a 20-case Integration Test Plan covering 5xx/429/timeout/malformed-payload/duplicate-delivery). Each deliverable is individually downloadable as a styled standalone HTML, prints to a matching PDF layout, and the two tabular docs export to CSV. Colaberry logo fetched from the official URL and embedded as base64; brand colors, fonts, and metadata script tag match the mandatory spec exactly.
  - Verification: Python `html.parser` validation — 0 parse errors across the full 332KB document; Node `new Function()` syntax check on the embedded `<script>` block — passed; all 27 inline SVG figures validated well-formed; `deepdive-metadata` JSON tag validated via `json.loads`; file opened in the default browser for visual confirmation.
  - Notes: Followed the CSS/JS architecture already validated in `QAEngineer_FieldGuide.html` and `UXDesigner_FieldGuide.html` (same design-system classes, same offline search/ask/download engine) for cross-guide consistency within the curriculum track, with new content authored fresh for the integration-engineering discipline. No secrets introduced; no `/backend`, `/frontend`, or `/scripts` code touched, so no `tsc --noEmit` gate applies.

- [x] Data Architect Field Guide (Week 9 deep dive)
  - Date: 2026-08-27
  - Session: CC-20260827-f8k3
  - What changed: Added `DataArchitect_FieldGuide.html` — a self-contained knowledge-base-style field guide (left topic nav, header search, offline "Ask the Guide" Q&A, light/dark theme) teaching data-architecture review judgment for an AI Solution Architect. Introduces a new invented initiative on top of the curriculum's existing Aegis Mutual / ACTA (Aegis Auto Claims Triage Assistant, Insurance) world already established by the QA, UX Design, and Integration Engineer field guides: the **Aegis Data Foundation (ADF)**, a governed warehouse unifying Policy Admin System (PAS), ACTA, and ClaimPay data into a dimensional star schema for actuarial, finance, and regulatory reporting. Contains 10 foundational-concept sections (conceptual/logical/physical models, ERD cardinality, normalization, OLTP/OLAP, ETL/ELT, quality & lineage, master data, governance/PII/retention, schema evolution, the review lens) plus all 9 requested deliverables: Data Model & ERD (crow's-foot ERD across 11 entities plus a star-schema diagram), a 87-field Data Dictionary, Physical Schema/DDL (12 CREATE TABLE statements — reference tables, SCD Type 2 dimensions, two grain-declared fact tables), Pipeline (ETL/ELT) Design with a flow diagram and a per-source idempotency table, an 18-rule Data Quality rule set (validity/completeness/uniqueness/consistency) with a pass-rate bar chart, a Governance & Retention Policy (field-level PII classification, access-role table, retention schedule, the insurance-recordkeeping deletion-request boundary), a Storage & Retention Strategy (OLTP/OLAP split, hot/warm/cold tiers with a donut chart and a cost-by-tier bar chart), a 10-KPI Data Lineage Map with a worked lineage-flow diagram, and a Master & Reference Data Definition (customer/vehicle golden-record survivorship rules plus all 4 shared code sets populated with real values). The three tabular deliverables (dictionary, quality rules, lineage map) export to CSV; every deliverable downloads as a standalone styled HTML and prints to a matching PDF layout. Colaberry logo extracted from the already-verified base64 embedded in `AIEngineer_FieldGuide.html` (not re-fetched or redrawn) and applied via a single shared `LOGO_DATA_URI` JS constant written into every doc cover at render time, plus two directly-embedded copies in the topbar/hero for pre-JS visibility; brand colors, fonts, and the `deepdive-metadata` script tag match the mandatory spec exactly.
  - Verification: Python `html.parser` validation — 0 parse errors across the full ~250KB document; Node `new Function()` syntax check on the embedded `<script>` block — passed; `deepdive-metadata` JSON tag validated via `json.loads`; automated checks confirmed zero duplicate element IDs, matched `<svg>`/`</svg>` counts, every `downloadDoc`/`printDoc` call target and every `downloadCSV` kind resolving to a real section id / supported export, and every sidebar `href="#..."` anchor resolving to an existing element id (22/22); file opened in the default browser for visual confirmation.
  - Notes: Caught and corrected a real defect mid-build: an early draft of the Doc 01 cover logo was accidentally filled with a fabricated/truncated base64 string instead of the verified Colaberry logo — found on self-review before publishing and replaced with the exact byte-for-byte string extracted from the already-shipped `AIEngineer_FieldGuide.html`, per the branding rule that the logo must never be invented or redrawn. Reused the CSS/JS architecture already validated across the QA, UX Design, Integration Engineer, and AI Engineer field guides for cross-guide consistency, with all data-architecture content authored fresh. No secrets introduced; no `/backend`, `/frontend`, or `/scripts` code touched, so no `tsc --noEmit` gate applies.

- [x] Governance Lead Field Guide (Week 11 deep dive)
  - Date: 2026-08-29
  - Session: CC-20260829-3b8c
  - What changed: Added `GovernanceLead_FieldGuide.html` — a self-contained knowledge-base-style field guide (left topic nav, header search, offline "Ask the guide" Q&A, light/dark theme) teaching AI-governance review judgment for an AI Solution Architect. Reuses the curriculum's existing Aegis Mutual / ACTA (Aegis Auto Claims Triage Assistant, Insurance) world already established by the QA, UX Design, Integration Engineer, and Data Architect field guides, viewed through the governance layer: who decides, who's accountable, what evidence proves a control ran. Contains 9 foundational-concept sections (governance vs. management, decision rights & RACI, policies/standards/controls, risk register & appetite, AI-specific risk categories, compliance mapping, audit trails & evidence, responsible AI & human oversight, operating model & cadence) plus 3 architect's-judgment sections (good vs. bad governance, governance KPIs with 6 live-value tiles, the 7-point review lens) plus all 7 requested deliverables: a Governance Framework (with a three-lines-of-defense SVG diagram), a 10-decision Decision RACI with exactly one Accountable role per row, a 20-obligation Compliance Checklist mapped to SOC 2 / ISO 27001 / GDPR (plus a bonus NAIC row) with a control-to-framework mapping diagram and an evidence column on every row, a 12-risk Risk Register scored inherent vs. residual against a Board-approved 3-tier numeric risk appetite with a 5×5 heatmap (inherent scores reaching 20; one residual — vendor concentration — deliberately left at "Accepted with Monitoring" rather than forced to zero), a Responsible-AI Policy with a full decision/escalation flowchart (fraud-score routing, Fast-Track auto-approval, denial sign-off) and a 4-trigger human-oversight table each with a numeric condition and a hard stop, an Audit Plan with 12 risk-weighted control tests and a control-coverage donut, and an Operating Model & Cadence with 4 standing bodies and an escalation-ladder diagram including an emergency kill-switch bypass path. The four tabular deliverables (RACI, Compliance, Risk, Audit) export to CSV; every deliverable downloads as a standalone styled HTML and prints to a matching PDF layout. Colaberry logo extracted byte-for-byte from the already-verified base64 in `DevOpsEngineer_FieldGuide.html` via a Node script (never retyped by hand) and embedded directly in every `img.needs-logo`/`.docx-logo` element; brand colors, fonts, and the `deepdive-metadata` script tag (week 11, per the task's own spec) match the mandatory spec exactly.
  - Verification: Python `html.parser` validation — 0 parse errors across the full ~195KB document; Node `new Function()` syntax check on the embedded `<script>` block — passed; `deepdive-metadata` JSON tag validated via `json.loads`; automated checks confirmed zero duplicate element IDs, matched 13/13 `<svg>`/`</svg>` tags, all 24 sidebar nav links resolving to 24 real section ids, every `downloadDoc`/`printDoc` call target resolving to a real `docxroot-*`/section id, every `downloadCSV` kind resolving to a real `CSV_DATA` key with header/row column counts matching exactly (raci 6×10, compliance 7×20, risk 12×12, audit 8×12, zero ragged rows) via a headless Node evaluation of the actual `CSV_DATA` object; file opened in the default browser for visual confirmation.
  - Notes: Caught and corrected a real defect mid-build, same failure mode flagged in the Data Architect entry above: my first attempt at the `LOGO_B64` JS constant was hand-retyped into the Edit call and came out truncated (2,844 characters instead of the correct 25,324) — caught immediately by a length check before publishing, then fixed by writing a small Node script that spliced the exact byte-for-byte string (extracted earlier into a scratch file via `sed`, never eyeballed or retyped) into the file and verified equal to the already-shipped DevOps guide's copy. Reused the CSS/JS architecture already validated across the QA, UX Design, Integration Engineer, Data Architect, and DevOps field guides for cross-guide consistency, with all governance content authored fresh and grounded in the exact numeric thresholds ($25,000 Fast-Track ceiling, confidence ≥0.90, SentinelScore 0–100 bands) already established in the QA and Data Architect guides, so this guide's RACI/risk/policy thresholds don't contradict prior curriculum artifacts. No secrets introduced; no `/backend`, `/frontend`, or `/scripts` code touched, so no `tsc --noEmit` gate applies.

## Command Center

- [x] Command Center — Overview tab (shell + Overview built, other 8 tabs stubbed)
  - Date: 2026-08-17
  - Session: CC-20260817-q2p9
  - What changed: Added `command-center/` (static site, no build step): `index.html`, `styles.css`, `app.js`, and `data.js` as the single data source. Overview tab is fully built: project description, release-you-are-in strip (r0/r1/r2 with computed status from today's date), a requirement-by-requirement "what's live" rollup (all 10 REQs, only REQ-008 marked built), the R4 guardrail foundational-work callout, and a system-connections panel that shows a grey "no systems connected yet" state in Real mode. The other 8 tabs (Outcomes, Users & Use Case, Guardrails, Systems, Project Management, AI Agents, Knowledge Base, Data Model) are reachable from the nav but render an honest "not built yet" stub naming what will live there and what has to happen first, per the plan's own instruction to stop after Overview. Global Real/Sample toggle wired (defaults to Real; Sample mode shows a persistent banner and clearly-tagged fabricated stats/connections). Colour palette is a documented neutral placeholder, defined once in `styles.css :root`.
  - Verification: `node --check` passed on `app.js` and `data.js`; re-ran `npm test` — 17/17 pass, used as the evidence backing the REQ-008 "built" claim; opened `command-center/index.html` in the default browser to confirm it renders with no build step.
  - Notes: No colours were chosen for this project yet, so the palette is a neutral placeholder in one place as instructed. No external systems are named in the plan, so Systems stays an empty state in both the Overview panel and its own stub. Stopping here per the plan's instruction to get Overview right before building the other eight tabs.
STORY-000 setup complete

- [x] Command Center — STORY-000 verification fixes (card drill-down, runtime data files, data-age warning)
  - Date: 2026-08-17
  - Session: CC-20260817-r5n8
  - What changed: Fixed the 3 gaps found by the STORY-000 verification pass. (1) Card drill-down: every `.cc-card` now has `cursor:pointer` and an `onclick` (event-delegated in `app.js`) that opens a one-level-down detail view in a new generic modal widget (`command-center/modal.js`); detail content for all 6 Overview cards and all 8 stub-tab cards is built from real fetched data only, no invented numbers. (2) Runtime data files: created `.colaberry/plan.json` (meta/guardrails/requirements/releases/stories/systems/users) and `.colaberry/progress.json` (foundationalWork/owners/connections) holding the data that used to be hard-coded in `data.js`; deleted `data.js`; added `command-center/data-loader.js` which fetches both files at runtime (5s timeout, typed errors: NetworkError/TimeoutError/UpstreamUnavailable/ContractViolation) and `command-center/sample-data.js` for the (intentionally still-JS) sample-mode fixture; `app.js` now loads data asynchronously on `init()` and shows a loading/error state instead of rendering hard-coded facts. Release-status math now uses the live current date instead of a frozen `TODAY` constant. (3) Data-age warning: created `.colaberry/manifest.json` (`dataUpdated` + per-source timestamps); added a persistent `#data-age-bar` (shown on every tab, in `index.html`/`app.js`/`styles.css`) that reads the manifest and displays "Data last updated <date> (<n> days ago)", switching to a warning style when the data is more than 7 days old. Added `scripts/serveCommandCenter.js` — a zero-dependency Node static file server (`node:http`/`node:fs`/`node:path` only, path-traversal guarded, binds to 127.0.0.1) because `fetch()` of local JSON is blocked when `index.html` is opened via `file://`; added `npm run serve:command-center`.
  - Verification: `node --check` passed on `app.js`, `data-loader.js`, `modal.js`, `sample-data.js`, `scripts/serveCommandCenter.js`; all 3 new JSON files parse via `JSON.parse`; re-ran `npm test` — 17/17 pass, unaffected by this change; started the server and confirmed via `curl` that `index.html`, `app.js`, and all three `.colaberry/*.json` files are served with correct content types and that a raw path-traversal request is rejected; ran an end-to-end Node script (using native `fetch` against the running server) that calls the real `loadCommandCenterData()`/`commandCenterDataAge()` functions and asserts the merged shape (10 requirements, 2 guardrails, 3 releases, 7 stories, 1 foundational-work entry, 3 owners, 0 connections/systems) and the staleness math (0 days → not stale, 9 days → stale) all match expectations; opened `http://127.0.0.1:4173/command-center/` in the user's default browser via the running server for visual confirmation (no Playwright/browser-automation tool available in this repo to self-verify the UI visually, so this is user-confirmable, not self-confirmed).
  - Notes: Sample-mode data intentionally stays a JS fixture (`sample-data.js`), not a `.colaberry/*.json` file — it's synthetic preview data, never real project state, and mixing it into the real-data read path would risk exactly the "invented number" failure mode STORY-000 flagged. `owners` and `connections` were assigned to `progress.json` (not `plan.json`) since they reflect who's currently doing what / what's currently connected, not the plan itself — a judgment call, logged per the assumption-logging rule. Awaiting user review before committing.

- [x] Command Center — fix static server directory routing (styles.css/modal.js/app.js 404s)
  - Date: 2026-08-17
  - Session: CC-20260817-r5n8
  - What changed: `scripts/serveCommandCenter.js` only special-cased the exact literal path `/` to map to `command-center/index.html`; any other directory-style request — including `/command-center/`, the URL the user was actually loading — fell through to the file-stat check, found a directory, and 404'd before the browser ever received HTML, so none of the page's script/link tags were ever fetched (that's why only some filenames showed in the console — nothing about the individual files was broken). Replaced the single hardcoded root case with a general `resolveFile()` that maps *any* requested directory (root or nested, with or without a trailing slash) to the `index.html` inside it, and changed all responses (including 404s) to `Cache-Control: no-store` so a stale error response can't linger in the browser cache across server restarts during dev.
  - Verification: `node --check` passed; killed the previously running server process and started a fresh one; `curl` confirms the user's exact URL (`http://127.0.0.1:4173/command-center/`, trailing slash) now returns `200 text/html`, and every sub-resource it references (`styles.css`, `sample-data.js`, `data-loader.js`, `modal.js`, `app.js`) plus all three `.colaberry/*.json` files return `200`; also confirmed the no-trailing-slash form (`/command-center`) now works, the bare root `/` still correctly 404s (no `index.html` at repo root), and the path-traversal guard still rejects `../../../..` escapes; re-ran `npm test` — 17/17 pass, untouched by this fix. Re-opened the URL in the user's browser; asked the user to hard-refresh since the earlier 404 may be cached.
  - Notes: Root cause was a routing gap, not a missing/misnamed file — every file the user listed already existed and was already being served correctly once requested directly by full path. Awaiting user confirmation that the hard-refreshed page now loads cleanly.

- [x] Command Center — record STORY-000 verification checklist in progress.json
  - Date: 2026-08-17
  - Session: CC-20260817-r5n8
  - What changed: User personally verified all 5 STORY-000 "Done means" items in the browser (tabs reachable + drill-down, sample data labelled, runtime read from plan.json/progress.json, manifest.json data-age warning, no fabricated data — the same 5 requirements verified twice earlier in this session). Added a new `storyVerifications` array to `.colaberry/progress.json` recording each item as `{ story: "STORY-000", item, confirmed: true, confirmedBy: "user", confirmedDate: "2026-08-17", method: "manual verification in browser" }`. No prior schema for this existed in the repo; field name and shape chosen to match the existing array-of-objects style already used for `foundationalWork`/`owners` — logged as an assumption.
  - Verification: `JSON.parse` confirms the file still parses; `git diff` reviewed and confirmed it touches only the intended lines.
  - Notes: `data-loader.js` only merges the known `foundationalWork`/`owners`/`connections` keys from `progress.json` into what the app renders, so this new field is inert data (not yet displayed anywhere) unless the app is later updated to read it — flagged to the user, no UI wiring requested or done.

- [x] STORY-000 — replace .colaberry/progress.json with the Colaberry platform's real schema
  - Date: 2026-08-17
  - Session: CC-20260817-r5n8
  - What changed: The prior `storyVerifications` guess didn't match what the Colaberry platform actually expects (it still showed "0 of 5 confirmed" after the last push). Escalated per CLAUDE.md's Escalation Protocol (`/tmp/escalation.json`) since no schema was documented anywhere in this repo; the DRI (Ali) then provided the real contract. Replaced the entire contents of `.colaberry/progress.json` with the platform's schema verbatim: `{ schema_version: 2, stories: [{ id: "STORY-000", criteria: [{ text, passed }, ...] }] }`, all 5 criteria marked `passed: true`. This removes the `foundationalWork`/`owners`/`connections` arrays the Command Center's `data-loader.js` was reading from this file, which would otherwise crash the Overview tab render (`real.foundationalWork.map is not a function` on `undefined`) — added a minimal defensive default (`progress.foundationalWork || []`, same for `owners`/`connections`) in `data-loader.js` so a missing key degrades to an empty state instead of crashing. `plan.json` (requirements, guardrails, releases, stories, users, systems) is untouched and still carries the REQ-008/R4-guardrail facts, so that information isn't lost from the app, just no longer duplicated in the now-empty "Foundational work shipped early" Overview card and the `agents` stub tab's owner list.
  - Verification: `JSON.parse` on the new `progress.json` — valid; `node --check` on `data-loader.js`; reproduced the crash first (confirmed `TypeError: Cannot read properties of undefined (reading 'map')` against the new schema with the old loader code), then re-ran the same end-to-end script against the live server confirming `foundationalWork`/`owners`/`connections` now resolve to `[]` with no crash and `plan.json`-sourced data (10 requirements) is unaffected; re-ran `npm test` — 17/17 pass, unaffected.
  - Notes: The emptied "Foundational work shipped early" card and `agents` stub detail list are a known, disclosed regression from this schema swap, not a bug — flagged to the user; no fix beyond the crash-prevention default was requested or made.

## r0 — Initial Workflow Skeleton

- [x] STORY-001 — Classify and Prioritize Support Requests (REQ-001, REQ-002)
  - Date: 2026-08-19
  - Session: CC-20260819-v3d6
  - What changed: Added `src/classify.js` exporting `classifySupportRequest(requestText)` — pure, deterministic, keyword-signal-based (no LLM call, per CLAUDE.md's "LLMs are probabilistic; production systems must be deterministic" principle). Classifies into the 7 predefined categories from REQ-001 (`login_problem`, `access_permission_issue`, `power_bi_report_issue`, `sql_database_issue`, `data_issue`, `technical_question`, `general_support_request`), determines a priority (`low`/`medium`/`high`/`urgent`) from separate urgency signals, and produces a whitespace-collapsed, length-capped summary — satisfying REQ-002. Unmatched/malformed input never throws: it falls back to `general_support_request` / `medium` with an empty summary, so "missing category" and "missing priority" are defined behaviors, not crashes. Every result carries a `logEntry` (Observability-Framework-shaped structured record: timestamp/level/service/event/outcome/context) satisfying the "should be logged in the audit trail" acceptance line at the walking-skeleton level — this only builds the record, it does not persist or write it anywhere; persistent audit trail storage is still STORY-003, not built here. Added `tests/classify.test.js`. Does not wrap output in the `guardrail.js` proposal shape — deciding how classification enters the human-approval flow is STORY-002, left unbuilt on purpose.
  - Verification: `npm test` — 31/31 pass (14 new: all 7 categories reachable, all 3 priority tiers + default-medium fallback, missing-category fallback, missing-priority fallback, malformed/empty input across 7 bad-input shapes never throws, deterministic tie-breaking between overlapping category signals, short-text and long-text summary truncation, structured log entry shape, purity/idempotency); the other 17 (guardrail + presentToAgent) unaffected.
  - Notes: Built as a paced co-pilot session per the story's own "how I want you to work" instruction — each of (1) module design, (2) implementation, (3) tests, (4) this PROGRESS.md/plan.json update was proposed and confirmed as a separate step, nothing batched. No secrets introduced; `/backend`, `/frontend` untouched (this is the standalone `src/` prototype tree, not the Colaberry monorepo layers), so no `tsc --noEmit` gate applies to plain JS here.

- [x] STORY-002 — Human Approval of Classification and Priority (REQ-005)
  - Date: 2026-08-19
  - Session: CC-20260819-x4k9
  - What changed: Added `src/reviewClassification.js` exporting `reviewClassification(classification, decision)` — a pure, deterministic review boundary for a support agent's approve/reject decision on a classification produced by `src/classify.js` (STORY-001, reused not rebuilt). `decision = { action: 'approve'|'reject', reviewer, reason? }`; `reviewer` is required (non-empty string) so every audit entry names who decided. Approve returns `{ ok:true, outcome:'approved', classification }`. Reject returns `{ ok:true, outcome:'rejected', status:'pending_reclassification', reason }` — the reclassification loop closes by the caller re-invoking `classifySupportRequest()` on the original request text (demonstrated in the test), not by this module re-implementing classification. Malformed input (bad classification shape, invalid `action`, missing/blank `reviewer`, non-object inputs) fails closed — `{ ok:false, outcome:'error', reason:'invalid_classification'|'invalid_decision' }` — and never throws, covering the "approval interface not loading" failure path. Every branch (approve, reject, error) always returns a structured `logEntry` (same Observability-Framework shape as STORY-001), satisfying "every approval or rejection action is logged in the audit trail" at the walking-skeleton level; this only builds the record, persistence is still STORY-003. Scope intentionally excludes "edit" — REQ-005's full text mentions it, but this story's acceptance criteria only test approve/reject, so edit is left for a future story rather than built speculatively. Added `tests/reviewClassification.test.js`. Updated `.colaberry/plan.json` (STORY-002 status → `built`, REQ-005 `built` → `true`, following the same builtBy-story convention already used for REQ-001/002/008) and `.colaberry/progress.json` (STORY-002 entry, all 3 criteria `passed: true`, verbatim acceptance-criteria text).
  - Verification: `npm test` — 46/46 pass (15 new: approve happy path + log shape, reject happy path + null-reason default + log shape, the reclassification-loop test that rejects a real `classifySupportRequest()` output and then re-classifies the same original text, malformed-classification fail-closed across missing-category/missing-priority/null/non-object/array inputs with a logged failure entry, malformed-decision fail-closed across invalid-action/missing-reviewer/blank-reviewer/non-object-decision, purity/idempotency); the other 31 (classify + guardrail + presentToAgent) unaffected. `JSON.parse` confirms both edited `.colaberry/*.json` files still parse.
  - Notes: REQ-005's full text also covers agent review of the *solution* and *draft response* (not just classification) — those artifacts don't exist yet (STORY-004/STORY-005). Marking REQ-005 `built:true` now follows plan.json's existing one-requirement-to-one-builtBy-story convention (matching how REQ-001/002 were marked built off STORY-001 alone), on the expectation that STORY-004/005 will feed their artifacts through this same review pattern rather than needing a new one; flagged here rather than silently assumed. Built as a paced co-pilot session per the story's instruction — design, implementation, tests, and this update were each proposed and confirmed as separate steps.

- [x] STORY-003 — Audit Trail Logging (REQ-010)
  - Date: 2026-08-20
  - Session: CC-20260820-q9tz
  - What changed: Added `src/auditLog.js` exporting `appendAuditEntry(logEntry, options?)` — the single sanctioned write path for the audit trail. Persists one hash-chained JSON record per line to `audit/audit-trail.log` (JSON Lines, append-only via `fs.appendFileSync` with the `'a'` flag, never rewritten). Each record carries `prevHash` (the previous record's `hash`, or a fixed genesis value for the first record) and its own `hash = sha256(prevHash + JSON.stringify(entry) + seq)`; editing or deleting any past line breaks the recomputed hash for every record after it, satisfying the "immutable, tamper-evident" design decision. On any write failure (malformed input, corrupted/unparseable chain tail, blocked/unwritable path) it never throws — it prints `ALERT: audit log write failed: <detail>` to stderr (no external alerting system exists yet, per the agreed design) and returns `{ ok:false, error:'audit_log_write_failed', message }`, fail-closed. Added `tests/auditLog.test.js`. Then added `src/auditedActions.js` exporting `classifyAndLog(requestText, options?)` and `reviewAndLog(classification, decision, options?)` — thin caller-side wrappers around the existing pure `classifySupportRequest()` (STORY-001) and `reviewClassification()` (STORY-002), calling each unchanged and then persisting the `logEntry` it already produced. `classify.js` and `reviewClassification.js` themselves were NOT touched — both are documented and tested as pure/side-effect-free, and adding a file write inside either would have broken that contract for every existing consumer; this follows the same thin-boundary shape `presentToAgent.js` already uses around `guardrail.js`. **`auditedActions.js` is scope added beyond STORY-003's story text, flagged explicitly, not silently expanded**: the story only asked to persist the `logEntry` objects `classify.js`/`reviewClassification.js` already build, but nothing in this repo called those pure functions in sequence outside of test files — without a caller, `appendAuditEntry()` would never run outside a test, and the acceptance line "given an action on a support request, when it occurs, then it is logged" would not be demonstrable. **Persistence currently happens only through `auditedActions.js`'s `classifyAndLog`/`reviewAndLog` — calling `classifySupportRequest()` or `reviewClassification()` directly (as existing callers/tests still do) does NOT write to the audit trail automatically.** A blocked/failed audit write does not block returning the underlying classification/review result (R4's human-approval gate is a separate concern from audit persistence) — the failure is surfaced via `auditResult.ok === false` plus the stderr alert. Added `tests/auditedActions.test.js`. Fixed a test-isolation bug found while wiring this in: `auditLog.test.js` and the new `auditedActions.test.js` initially shared the literal `tests/tmp` directory; since `node --test` runs files concurrently in separate processes, one file's cleanup could delete the directory mid-use by the other. Each file now uses its own unique subdirectory (`tests/tmp/auditLog-<uuid>/`, `tests/tmp/auditedActions-<uuid>/`). Updated `.colaberry/plan.json` (REQ-010 `enforced` → `true` with `mechanism`/`evidence`/`lastVerified`, STORY-003 status → `built`) and `.colaberry/progress.json` (STORY-003 entry, all 3 acceptance criteria `passed: true`, verbatim text), following the same convention STORY-001/002 used.
  - Verification: `npm test` — 68/68 pass (22 new: 13 in `auditLog.test.js` — first-entry genesis chaining, on-disk persistence, prevHash chaining across entries, nested-directory creation, no dedup on repeat identical entries, stored-hash recomputation, tamper detection via a hand-edited line, malformed-entry/corrupted-tail/blocked-path failures all fail-closed with an `ALERT:` stderr line, a healthy append prints nothing to stderr; 9 in `auditedActions.test.js` — shape parity with the unwrapped functions, persistence on both successful and failed classifications, approve/reject persistence, malformed-decision failure still logged, a classify-then-review sequence producing two correctly-chained records in order, a blocked audit write still returning a usable classification result); the other 46 (classify + guardrail + presentToAgent + reviewClassification) unaffected. Re-ran the full suite 3 times in a row after the tmp-dir race fix to confirm no remaining flakiness — 68/68 all three times. `JSON.parse` confirms both edited `.colaberry/*.json` files still parse.
  - Notes: `audit/audit-trail.log` does not exist in the repo yet — no real (non-test) invocation of `classifyAndLog`/`reviewAndLog` has happened, since this repo has no runtime entry point/orchestrator yet (STORY-001/002/003 are all still called only from tests). The file will be created on first real append; `*.log` is already gitignored so it won't get committed. Built as a paced co-pilot session per the story's instruction — module design, tests, the `auditedActions.js` scope call-out, and this update were each proposed and confirmed as separate steps.

## Prompt Library — `prompts/`

Standalone prompt-engineering tooling alongside the main `src/` build. Each prompt gets its own `prompts/<verb-noun-name>/` folder with an `eval.jsonl` (test cases, human-confirmed expected answers) and a `prompt.txt` (the prompt itself, not yet written for any prompt).

- [x] Add `scripts/score_prompt.py` — eval harness for prompt library
  - Date: 2026-08-22
  - Session: CC-20260822-p8f3
  - What changed: Added `scripts/score_prompt.py`, a standalone Python CLI (`python scripts/score_prompt.py <prompt_file> <eval_jsonl_file>`) that fills each eval case's `input` fields into `{{field}}` placeholders in a prompt file, sends the result to Claude via the `anthropic` SDK, parses the reply as JSON (tolerating a ```json fenced block), and grades it against the case's `expected` object — comparing only the named fields, case-insensitive/whitespace-trimmed for text, small-tolerance (0.01) for numbers, unparseable-JSON counted as a failed case rather than a crash. Prints a 0.0–1.0 score, one line per failure showing expected vs. actual, and the model name/case count used. Model name (`claude-sonnet-4-6`) is a single named constant (`MODEL_NAME`) at the top of the file, not buried in logic. `.env` loading is a small hand-written parser (no new dependency) reading `ANTHROPIC_API_KEY` from a project-root `.env`; missing/invalid key and a missing prompt or eval file each produce a plain-English message and a clean exit instead of a stack trace or a silent partial run. Also created `prompts/classify-issue-type/eval.jsonl` (5 human-confirmed cases: 4 ordinary + 1 deliberately ambiguous case that the user resolved by adding a 6th category, `needs-clarification`, to the `issue_type` enum) in an earlier step of this same session.
  - Verification: Ran `python scripts\score_prompt.py prompts\classify-issue-type\prompt.txt prompts\classify-issue-type\eval.jsonl` — since `prompt.txt` does not exist yet (expected; the prompt itself hasn't been written), the script correctly printed the plain-English "can't find a prompt file" message and exited cleanly (exit code 1), reaching that check before ever touching the API key or eval file, confirmed by the user's own instruction that this exact failure was anticipated. `anthropic` 0.120.2 confirmed already installed (`python -m pip show anthropic`); no `.env` exists yet in this repo, so the API-key path is unexercised pending the user adding one.
  - Notes: `MODEL_NAME = "claude-sonnet-4-6"` was set per explicit user instruction; this does not match any model ID in this session's own system context (`claude-fable-5`, `claude-opus-5`, `claude-sonnet-5`, `claude-haiku-4-5-20251001`) — flagged to the user, not silently substituted, since the constant is a one-line change if the real API rejects it. `tsc --noEmit` does not apply (plain Python, not the TypeScript backend/frontend layers). No secrets introduced — no `.env` was created or committed by this change.

- [x] Fix `MODEL_NAME` constant in `scripts/score_prompt.py`
  - Date: 2026-08-22
  - Session: CC-20260822-p8f3
  - What changed: Changed the `MODEL_NAME` constant at the top of `scripts/score_prompt.py` from `"claude-sonnet-4-6"` (flagged as unrecognized in the prior entry's Notes) to `"claude-sonnet-5"`, per explicit user confirmation that this is the correct current model ID. Single-line change, nothing else in the file touched.
  - Verification: `python -c "import ast; ast.parse(open('scripts/score_prompt.py').read())"` — file still parses as valid Python; diff confirmed to touch only the one constant line.
  - Notes: Resolves the discrepancy flagged in the previous entry.

- [x] Write `prompts/classify-issue-type/v1.0.0.md` (draft, rung 2)
  - Date: 2026-08-22
  - Session: CC-20260822-p8f3
  - What changed: Added the first version of the `classify-issue-type` prompt. Planned in Plan Mode first (plan approved before writing): deliberately stopped at rung 2 of the five-rung prompt ladder (Clear+direct → **Specific** → XML structure → Examples → Decomposition) rather than opening at a more elaborate version, per explicit instruction to start at the lowest rung likely to work and let the eval score reveal whether more is needed. Rung 1 was ruled out because it can't guarantee the JSON output shape `score_prompt.py` requires; rungs 3–5 (XML tagging, worked examples, scratchpad reasoning) were deferred as unearned costs — no eval case tests adversarial input, no evidence yet of category confusion, and this is a single closed-set decision that doesn't need visible step-by-step reasoning. File has a header block (name/version 1.0.0/purpose/model `claude-sonnet-5`/expects `{{request_id}}`+`{{message}}`/returns `issue_type`/status `draft`), a body with the 6-value category enum (5 real categories plus `needs-clarification`, matching the case-5 decision the user made when confirming `eval.jsonl`) and explicit rules for the three awkward situations (missing information → still pick best-fit category; ambiguous → pick the category tied to the primary ask; nothing to report → `needs-clarification`), and a closing return-contract line whose single field (`issue_type`) matches every `"expected"` object in `eval.jsonl` field-for-field.
  - Verification: Manually diffed the prompt's return-contract line against all 5 lines of `prompts/classify-issue-type/eval.jsonl` — field name and every enum value match exactly. Not yet run through `scripts/score_prompt.py` (no `.env`/API key configured in this repo yet) — actually scoring this draft against the eval is the deliberate next step, not done in this change.
  - Notes: Status is `draft` and intentionally left there — not marked ready for anything, per instruction. Whether to climb the ladder (add XML structure, examples, or a scratchpad) is a decision to make from the eval score, not from this file alone.

- [x] Score v1.0.0, add hard eval cases, fix one gap in v1.1.0.md
  - Date: 2026-08-22
  - Session: CC-20260822-p8f3
  - What changed: First real scoring run was blocked by a misnamed secrets file — `.env.txt` existed at the project root instead of `.env` (a Windows save-dialog artifact), so `score_prompt.py`'s loader never found `ANTHROPIC_API_KEY`. Confirmed the file held the right variable name (content checked with the value regex-redacted before ever being shown, and confirmed `.env` is covered by `.gitignore`) and renamed it — no key value was ever written, generated, or displayed by Claude. With the key in place, ran `score_prompt.py` on `v1.0.0.md` against the original 5-case `eval.jsonl`: 5/5 (1.00), nothing to fix. Per user decision, added 3 human-confirmed harder cases to `eval.jsonl` before touching the prompt: `REQ-2001` (a prompt-injection attempt embedded in the message, expected `assignment-logistics` — the real ask underneath), `REQ-2002` (two distinct, separately-actionable requests bundled in one message, expected `needs-clarification`), `REQ-2003` (incoherent/garbled text, expected `needs-clarification`). Re-scored `v1.0.0.md` against all 8 cases: 7/8 (0.88) — only `REQ-2002` failed, returning `coding-error` instead of `needs-clarification`; `REQ-2001` (injection) and `REQ-2003` (gibberish) both passed, showing rung 2 already resists injection without needing rung 3's XML tags. Diagnosed the single cause: the prompt's "Ambiguous" rule only defined `needs-clarification` as the fallback for "can't tell what they want," not for "can tell, but there are two separate requests." Made exactly one change: added a clause to that one rule in a new `prompts/classify-issue-type/v1.1.0.md` (version bumped to 1.1.0, everything else byte-identical to v1.0.0) telling the model to use `needs-clarification` when a message bundles two or more distinct, separately-actionable requests. `v1.0.0.md` was left completely untouched, per the rule that a version with a score is never overwritten. Scored `v1.1.0.md` against the same 8 cases: 8/8 (1.00).
  - Verification: All three scoring runs shown in full to the user (`score_prompt.py` output, not summarized or predicted): v1.0.0/5-case → 1.00, v1.0.0/8-case → 0.88 with the one named failure, v1.1.0/8-case → 1.00. `diff`-equivalent check (manual read) confirms `v1.1.0.md` differs from `v1.0.0.md` in exactly the version number and the one "Ambiguous" bullet.
  - Notes: Both `v1.0.0.md` and `v1.1.0.md` remain `Status: draft`. `.env`/`.env.txt` is a gitignored secrets file, not shipped code — the rename itself is intentionally not itemized further here beyond confirming it happened and no key value was exposed, per this repo's secrets-handling rules.

**Note (2026-08-22, Session CC-20260822-p8f3):** Rung 2 held against 8 eval cases including a prompt-injection attempt; climb to rung 3 (XML structure) only if a future case reveals it's actually needed. Don't add scaffolding on a hunch.

## r1 — Knowledge Base Integration and Response Drafting

- [x] STORY-004 — Search Knowledge Base for Troubleshooting Steps (REQ-003, REQ-009)
  - Date: 2026-08-22
  - Session: CC-20260822-b3w6
  - What changed: Added `src/data/knowledgeBase.json` — a small, hand-authored, on-disk knowledge base (REQ-009: "read from a small knowledge base") of 7 approved troubleshooting articles (`{ id, category, tags, title, steps }`), covering 6 of `classify.js`'s (STORY-001) 7 categories; `general_support_request` intentionally has no article since it's the catch-all fallback with no useful generic fix. Added `src/knowledgeBaseSearch.js` exporting `searchKnowledgeBase(classification, options?)` — takes a `classifySupportRequest()` output, validates it (fails closed on a missing/unrecognized category, never throws), loads the KB file with an explicit read timeout (`Promise.race`, default 5s), and scores each article by exact category match (+3) plus tag overlap with `classification.matchedSignals` (+1 each). Below a relevance threshold it returns `{ found:false, confidence:'none' }` with an explicit uncertainty message (REQ-003's "no relevant steps found" acceptance line); a weak match (tag overlap only, no category match) is still returned but flagged `confidence:'low'` with a human-review caveat rather than presented as certain — this is the guard against the "incorrect search results" failure path. Distinguishes `KnowledgeBaseUnavailableError` (missing file), `KnowledgeBaseAccessDeniedError` (permission denied), `KnowledgeBaseCorruptError` (invalid JSON or wrong shape), and `KnowledgeBaseTimeoutError` (read exceeded the timeout) as distinct `error_class` values, per the Observability Framework's "no generic Error in production paths" rule. Like `classify.js`/`reviewClassification.js`, this module only builds a structured `logEntry` (query = category + matchedSignals, plus result ids/confidence) — it does not persist to the audit trail itself. Extended `src/auditedActions.js` with `searchKnowledgeBaseAndLog(classification, options?)`, the same thin caller-side wrapper shape as `classifyAndLog`/`reviewAndLog`, which persists that `logEntry` via `auditLog.js`'s existing hash-chained, fail-closed writer (STORY-003, reused not rebuilt) — this is what makes REQ-003/REQ-009's "search query and results are logged in the audit trail" acceptance line true in practice, not just in the returned object. Added `tests/knowledgeBaseSearch.test.js` (16 new tests). This story recommends only — it never sends, resolves, or modifies anything, so `guardrail.js` (R4) is untouched; searching isn't one of `RESTRICTED_ACTIONS`.
  - Verification: `npm test` — 84/84 pass (16 new: relevant-match happy path for a real classified request, category-only medium-confidence match, no-match uncertainty, low-confidence caveat on a weak tag-only match, malformed classification fail-closed across missing/unrecognized-category/non-object inputs never throwing, missing KB file, corrupt/unparseable KB file, wrong-shape KB file, access-denied read via an injected `readFile`, timeout via an injected slow `readFile` against a 5ms budget, logEntry captures query+result ids, `searchKnowledgeBaseAndLog` persists a search entry to the audit trail, a malformed search still logs a failure entry, idempotency); the other 68 (classify + guardrail + presentToAgent + reviewClassification + auditLog + auditedActions) unaffected. Re-ran the full suite 3 times in a row — 84/84 all three times, no flakiness (the timeout test uses an injected slow promise on a controlled clock, not real disk-read timing, so it isn't racy). `node --check` passed on both new/changed `.js` files; `JSON.parse` confirms `knowledgeBase.json` parses.
  - Notes: Checked `.colaberry/plan.json` and `.colaberry/progress.json` before touching them — unlike what earlier entries here described for STORY-001–003 ("plan.json status → built", "REQ built → true"), the *current* `plan.json` schema has no `status`/`built` fields at all, and `progress.json`'s `verification` block (`commit_sha`, `verified_at`, `points_awarded`) for STORY-001–003 already holds real values that match the platform's webhook sync after each commit, not hand-typed data. Deliberately left both files untouched rather than guessing a new field shape into them — the STORY-000 history earlier in this file is the exact cautionary tale for that mistake. `searchKnowledgeBase` is deliberately not pure like `classify.js`/`reviewClassification.js` (it reads a file) but has no side effects (no writes) and is deterministic for a fixed KB file — documented explicitly in the module header since it's a real deviation from the established pattern. Built as a paced co-pilot session per the story's instruction — KB data design, the search module, the audited wrapper, and each test/verification step were proposed and confirmed one at a time, nothing batched. No secrets introduced; `tsc --noEmit` does not apply (plain JS `src/` prototype tree, not the TypeScript backend/frontend layers described elsewhere in CLAUDE.md).

- [x] STORY-005 — Generate Draft Response for Agent Review (REQ-004)
  - Date: 2026-08-23
  - Session: CC-20260823-q4t8
  - What changed: Added `src/data/responseTemplates.json` — a small, hand-authored template file with one entry per specific-issue category `classify.js` (STORY-001) recognizes, plus a mandatory `default` fallback (`general_support_request` intentionally has no entry of its own, same catch-all convention `knowledgeBase.json` already established in STORY-004), a shared greeting/sign-off, and a `stepsSection` block for the found/low-confidence/not-found branches. Added `src/generateDraftResponse.js` exporting `generateDraftResponse(classification, kbSearchResult, options?)` — takes a `classify.js` classification and a `searchKnowledgeBase()` result (STORY-004, reused not rebuilt) and assembles a professional draft by template substitution only, no model call, per CLAUDE.md's core principle ("LLMs are probabilistic. Production systems must be deterministic.") already cited in `classify.js`'s own header and already followed by `knowledgeBaseSearch.js`. Mirrors `knowledgeBaseSearch.js`'s I/O pattern exactly: async `readFile` raced against an explicit timeout (`Promise.race`, default 5s) rather than a bare sync read, so a local template-file read is treated the same "external boundary" way the KB read already is, per Failure-First Design. Validates the classification and fails closed on a missing/unrecognized category (`ValidationError`, "Response not generated"); distinguishes `TemplateUnavailableError` (missing file), `TemplateAccessDeniedError` (permission denied), `TemplateCorruptError` (invalid JSON or wrong shape), and `TemplateMissingError` (neither the category's template nor `default` exists) as the "Template missing" failure path's distinct `error_class` values, per the Observability Framework's no-generic-`Error` rule; after rendering, self-checks the assembled text for a leftover unresolved `{{...}}` placeholder or an empty body and fails closed as `ResponseRenderError` if either is found — the concrete handling for "Response contains errors". The returned draft is always `{ editable: true, draftText: <plain string> }` with no lock/sent/approved flag — the concrete handling for "Response not editable" — and this module never touches `guardrail.js`/`presentToAgent.js`, same reasoning `knowledgeBaseSearch.js` already documented: producing an editable draft for a human to review is not one of R4's `RESTRICTED_ACTIONS` (`send_message` etc.); only actually sending it would be. Extended `src/auditedActions.js` with `generateDraftResponseAndLog(classification, kbSearchResult, options?)`, the same thin caller-side wrapper shape as the other three actions, persisting the logEntry via `auditLog.js`'s existing hash-chained, fail-closed writer — this is what makes "Trust: draft generation should be logged in the audit trail" true in practice. Added `tests/generateDraftResponse.test.js` (21 new tests, including the two `generateDraftResponseAndLog` audit-persistence cases — co-located here rather than in `tests/auditedActions.test.js`, matching the precedent `tests/knowledgeBaseSearch.test.js` already set for `searchKnowledgeBaseAndLog`, not the file I first (incorrectly) proposed).
  - Verification: `npm test` — 105/105 pass (21 new: high-confidence KB match happy path with numbered steps, summary sentence included when present, low-confidence caveat line, "not found" branch with no fabricated steps, `general_support_request` falling back to `default`, a missing `kbSearchResult` treated as not-found rather than throwing, malformed classification fail-closed across missing/unrecognized-category/null/non-object/array inputs never throwing, missing/corrupt/wrong-shape/access-denied/timeout template reads each surfacing the correct distinct `error_class`, a category with neither its own template nor `default` surfacing `TemplateMissingError`, a stray unresolved placeholder surfacing `ResponseRenderError`, an `editable`/plain-string contract check confirming the draft carries no `sent`/`approved`/`locked` flag and is freely mutable, `logEntry` shape, idempotency, and the two `generateDraftResponseAndLog` audit-trail persistence cases — success and malformed-classification-still-logged); the other 84 (classify + guardrail + presentToAgent + reviewClassification + auditLog + auditedActions + knowledgeBaseSearch) unaffected.
  - Notes: Checked `.colaberry/progress.json` before touching it, same as the STORY-004 precedent — STORY-005's entry is already present with `state: "not_started"` and `criteria.passed: false`, matching the STORY-000-verified pattern where this file's `verification` block gets filled by the platform's own webhook sync after the commit lands, not by hand-editing. Left `.colaberry/plan.json` and `.colaberry/progress.json` untouched for the same reason as STORY-004. Two self-corrections made mid-session and flagged to the user as they happened: (1) switched the template-file read from a proposed sync `readFileSync` to the async-`readFile`-plus-timeout pattern to match `knowledgeBaseSearch.js`'s established precedent for local data-file reads; (2) moved the `generateDraftResponseAndLog` tests from a first-proposed `tests/auditedActions.test.js` to `tests/generateDraftResponse.test.js`, matching where the equivalent `searchKnowledgeBaseAndLog` tests actually live. Built as a paced co-pilot session per the story's instruction — template design, the draft module, the audited wrapper, and each test/verification step were proposed and confirmed one at a time, nothing batched. No secrets introduced; `tsc --noEmit` does not apply (plain JS `src/` prototype tree).

## r2 — Escalation and Final Summary

- [x] STORY-006 — Recommend Escalation with Explanation (REQ-006)
  - Date: 2026-08-23
  - Session: CC-20260823-h2v9
  - What changed: Added `src/generateEscalationRecommendation.js` exporting `generateEscalationRecommendation(classification, kbSearchResult)` — a pure, deterministic decision (no model call, per CLAUDE.md's core principle already cited throughout this repo's `src/` modules) that recommends escalation only when a prior `searchKnowledgeBase()` call (STORY-004, reused not rebuilt) came back `found: false` — REQ-006's literal text, "cannot be solved using the available knowledge." `found: false` deliberately covers both a genuine no-match and a KB search failure (missing/corrupt/timed-out file), since either way the assistant can't confirm an answer and escalating is the safe fail-closed default; a low-confidence-but-found match is deliberately NOT escalated (that's already flagged for human review at the STORY-005 draft-response stage) — a scope decision logged in the module header, not a silent guess. Every recommendation carries a non-null `explanation` (recommended or not — guards "Explanation missing"), and when `recommended: true` the module also builds a `guardrail.js`-shaped `action` (`type: 'escalate'`, `requiresApproval: true`, `status: 'proposed'`) — `escalate` was already listed in `guardrail.js`'s `RESTRICTED_ACTIONS` ahead of this story, so this is the first module whose output is meant to actually pass through the R4 boundary (`validateAssistantOutput`/`presentToAgent.js`), not just sit beside it; verified directly with an integration test, not just asserted by shape. Added `src/reviewEscalation.js` exporting `reviewEscalation(recommendation, decision)` — the agent's approve/reject of that recommendation, mirroring `reviewClassification.js`'s (STORY-002) shape exactly. It only accepts a recommendation that is actually `recommended: true` with a correctly guardrail-shaped `action`; anything else (malformed, or a not-recommended result passed in by mistake) fails closed with `invalid_recommendation` — the concrete handling for "Approval interface not available." Approve/reject only record the human's decision; neither performs an actual escalation against an external system, since no ticketing integration exists in this repo yet (same boundary `reviewClassification.js`'s approve already respects — R4 forbids the assistant from escalating a case itself regardless of who approved the recommendation). Extended `src/auditedActions.js` with `generateEscalationRecommendationAndLog(classification, kbSearchResult, options?)` and `reviewEscalationAndLog(recommendation, decision, options?)`, the same thin caller-side wrapper shape as the four existing actions, persisting each logEntry via `auditLog.js`'s existing hash-chained, fail-closed writer — this is what makes "Trust: the escalation recommendation and decision should be logged in the audit trail" true in practice. Added `tests/generateEscalationRecommendation.test.js` (20 new tests, including the guardrail/presentToAgent integration checks and the two `generateEscalationRecommendationAndLog` audit-persistence cases) and `tests/reviewEscalation.test.js` (18 new tests, including the two `reviewEscalationAndLog` audit-persistence cases) — both `AndLog` test pairs co-located with their own resource file, matching the precedent `tests/generateDraftResponse.test.js` already set, not `tests/auditedActions.test.js`.
  - Verification: `npm test` — 143/143 pass (38 new across the two files: escalation recommended on KB `found:false` (including a KB-search-failure case), explanation always present and category-specific, action shape matches guardrail exactly, the action verified live against both `validateAssistantOutput` and `presentToAgent` (safe, unblocked, surfaced as `pendingApproval` not auto-executed), not-recommended on a found match and on a low-confidence match, malformed-classification and malformed-kbSearchResult fail-closed across missing/unrecognized/null/non-object/array inputs never throwing, logEntry shape on both branches, purity/idempotency; reviewEscalation: approve/reject happy paths, approve confirmed to add no `executed`/`escalated`/status-changing flag, a not-recommended result refused as unreviewable, a tampered/incomplete `action` refused, malformed-decision fail-closed across invalid-action/missing-reviewer/blank-reviewer/non-object inputs, purity/idempotency; both `AndLog` wrappers confirmed to write a real record to a real audit-log file and to still log on the failure path); the other 105 (classify + guardrail + presentToAgent + reviewClassification + auditLog + auditedActions + knowledgeBaseSearch + generateDraftResponse) unaffected. Re-ran the full suite 3 times in a row — 143/143 all three times, no flakiness (each new test file uses its own unique `tests/tmp/<name>-<uuid>/` subdirectory, same fix STORY-003 already established for the concurrent-file-runner race). `node --check` passed on `generateEscalationRecommendation.js`, `reviewEscalation.js`, and the modified `auditedActions.js`.
  - Notes: `.colaberry/plan.json` has an uncommitted schema-sync modification already sitting in the working tree at session start (not from this session) that independently defines STORY-006 with the same acceptance criteria and failure paths given in this story's brief, word-for-word — left untouched, same reasoning STORY-004/005 already documented (the platform owns that sync; `.colaberry/progress.json`'s `verification` block gets filled after the commit, not by hand-editing). No downstream escalation execution (e.g., a real ticketing-system call) exists in this repo — `reviewEscalation.js`'s approve only records the human decision, which is the correct stopping point per R4 and per this story's own scope (STORY-007, final summary, is explicitly next and was not built here). Built as a paced co-pilot session per the story's instruction — each of (1) `generateEscalationRecommendation.js` design, (2) its tests, (3) `reviewEscalation.js` design, (4) its tests, (5) the `auditedActions.js` wiring, (6) this update, was proposed and confirmed as a separate step, nothing batched. No secrets introduced; `tsc --noEmit` does not apply (plain JS `src/` prototype tree, not the TypeScript backend/frontend layers described elsewhere in CLAUDE.md).

- [x] Sync `.colaberry/plan.json` to the platform's `schema_version: 2` shape, then fix the Command Center to match
  - Date: 2026-08-23
  - Session: CC-20260823-h2v9
  - What changed: The uncommitted `.colaberry/plan.json` schema-sync change noted in the STORY-006 entry above (already sitting in the working tree at session start, not authored this session) was committed on its own after the user confirmed they wanted it kept, separately from the STORY-006 code commit — it's a data/config sync, not code. Validated with `JSON.parse` before committing. That sync then broke the Command Center: the user reported `Uncaught (in promise) TypeError: Cannot read properties of undefined (reading 'map') at renderOverview (app.js:372:34)`. Root cause: `command-center/data-loader.js` still copied the old top-level `plan.json` keys (`meta`, `guardrails`, `systems`, `users`) straight onto `real`, none of which exist under the new `schema_version: 2` shape — `real.users` was `undefined`, so `real.users.map(...)` threw. The old field names on requirements (`type`/`text`/`builtBy`/`built`), stories (`owner`/`due`/`status`), and releases (`id`/`start`/`end`/`storyCount`) were gone too, replaced by `kind`/`statement`/`fulfilled_by`, `owner_agent`/`due_on`, and `key`/`starts_on`/`ends_on` — and neither `plan.json` nor `progress.json` carries a `built`/`status` field anymore at all. Diagnosed and explained to the user first (without fixing) per their explicit request, then fixed on a separate go-ahead using a field mapping the user supplied. Rewrote `data-loader.js`: it now builds a `progressStoriesById` index from `progress.json`'s `stories[].verification.state` and joins it onto `plan.json`'s stories (adding a computed `status`, defaulting to `'not_started'` for any story not yet in `progress.json` rather than crashing or silently counting as done) and requirements (adding a computed `built`, true only when `fulfilled_by` is non-empty and every listed story's status is `'verified'`); relocated `real.project = plan.project` (was `plan.meta`), `real.guardrails = plan.derived.guardrails`, `real.systems = plan.derived.systems`, `real.users = plan.derived.roles`. Updated every site in `app.js` that read an old field name (`real.meta.*` → `real.project.*` with `tagline` → `descriptor`, three `real.users` render sites adapted from `{role, wants}` objects to plain role-name strings since the `wants` description text no longer exists in the source data, `g.reqId`/`g.text` → `g.id`/`g.statement` with the now-absent `mechanism`/`evidence`/`enforced` pill dropped from rendering rather than fabricated, `r.type`/`r.text`/`r.builtBy` → `r.kind`/`r.statement`/`r.fulfilled_by.join(', ')`, `s.owner`/`s.due` → `s.owner_agent`/`s.due_on`, `s.status === 'done'` → `s.status === 'verified'` (this also fixes a pre-existing dead comparison — old story status values were never actually `'done'`), `r.id`/`r.start`/`r.end` → `r.key`/`r.starts_on`/`r.ends_on`, and `r.storyCount` (field no longer exists) replaced with a live count computed from `real.stories`). Added a null-date guard in `releaseStatus()`/`currentOrNextRelease()` so a release with no `starts_on`/`ends_on` yet (the current state of all three releases in this sync) renders `'unscheduled'` instead of falling through to a false `'complete'`, and a guard around the Overview footer's demo-prep text so a missing `demoDay`/`buildEnds` (also currently missing — `plan.project` carries no date fields and top-level `plan.schedule` is `null`) renders "not yet scheduled" instead of computing `NaN days`.
  - Verification: `node --check` passed on both files. Ran the real `loadCommandCenterData()` end-to-end against the live `.colaberry/*.json` files through the actual running server (`node scripts/serveCommandCenter.js`) via a Node script using native `fetch` — confirmed `REQ-001/002/003/004/005/008/009/010` compute `built: true` and `REQ-006/007` compute `built: false` (their stories aren't `verified` yet), and all 7 stories' computed `status` match `progress.json`'s `verification.state` exactly (6 `verified`, `STORY-006`/`STORY-007` `in_progress`). Separately traced every `renderOverview` computation (re-implementing the same pure helpers verbatim) against that same real data and asserted no `NaN`/`undefined` substring appears in any rendered string — confirmed the null-date releases correctly show `unscheduled` and the missing demo/build dates correctly show the "not yet scheduled" fallback text rather than garbage. Re-ran `npm test` — 143/143 pass, unaffected (separate from `command-center/`). No browser-automation tool exists in this repo (same documented gap as the STORY-000 verification entry above) — this is a thorough data/logic-layer self-check, not a visual browser confirmation; flagged to the user as still worth an eyeball.
  - Notes: Two things dropped from the UI because the new schema no longer carries the underlying data, not fixed to just clear a crash: (1) each role's descriptive "wants" text (previously `{role, wants}`, now `plan.derived.roles` is a plain array of role-name strings) — the Overview and Users-tab role lists now show just the role name; (2) each guardrail's `mechanism`/`evidence`/`enforced` detail (previously on `plan.guardrails`, now `plan.derived.guardrails` is just `{id, statement}`) — guardrail cards now show only the requirement id and statement text. Both are disclosed regressions from the platform's schema migration, not bugs introduced here, and nothing was invented to paper over either gap, consistent with this project's no-fabricated-data rule (see the STORY-000 "replace progress.json" entry above for the same principle applied previously). `.colaberry/plan.json` was committed separately (`5e3d92d`) from this `data-loader.js`/`app.js` fix (`2fedfc8`) and from the STORY-006 code commit (`7b3fc50`), per explicit user direction to keep the data/config sync, the story code, and the dashboard-compatibility fix as three distinct commits.

- [x] STORY-007 — Generate Final Support Summary (REQ-007)
  - Date: 2026-08-24
  - Session: CC-20260824-r7x2
  - What changed: Added `src/generateSupportSummary.js` exporting `generateSupportSummary(workflow)` — a pure, deterministic (no model call, per CLAUDE.md's core principle already cited throughout `src/`) compiler that assembles a final, human-readable document from the artifacts already produced elsewhere in this repo's workflow: `classify.js` (STORY-001) + `reviewClassification.js` (STORY-002) + `knowledgeBaseSearch.js` (STORY-004, optional) + `generateDraftResponse.js` (STORY-005) + `generateEscalationRecommendation.js`/`reviewEscalation.js` (STORY-006, present only when an escalation actually happened) — all reused, none rebuilt. This is the first story to need a bundled "workflow" shape tying one ticket's artifacts together (`ticketId`, `requestText`, plus those five prior results); no earlier story defined that shape, so it's introduced here and flagged, not silently assumed — confirmed with the user before building. Validates every required piece is present and internally consistent before compiling: fails closed (`generated:false`, no fabricated text, `ValidationError`) on a missing/blank `ticketId`, missing `requestText`, or a malformed classification/review/draftResponse; degrades gracefully (not a hard failure) when `kbSearchResult` is absent, noting "no knowledge-base search recorded" rather than fabricating one — the concrete handling for "Summary incomplete". A dedicated `EscalationMismatchError` class guards "Incorrect summary details": the module refuses to compile a summary claiming an escalation decision unless the `escalationReview` actually references the same `escalationRecommendation` object passed alongside it (checked by structural equality), and refuses any escalation review/recommendation pairing that doesn't line up (a review with no recommendation, a recommended-but-unreviewed escalation, or a not-recommended escalation carrying a stray review). Never touches `guardrail.js` — compiling a read-only summary isn't one of R4's `RESTRICTED_ACTIONS`. Added `src/saveSupportSummary.js` exporting `saveSupportSummary(summary, options)` — the deliberate, separate **manual** save action (never auto-invoked by `generateSupportSummary`, which is what makes "I should be able to save it manually" true rather than automatic), writing one JSON file per ticket to `summaries/<ticketId>.json`. Mirrors `auditLog.js`'s sync-write, fail-loud style (this repo's other write boundary) rather than the async-read pattern `knowledgeBaseSearch.js`/`generateDraftResponse.js` use, since this is a write not a read. Saving is an upsert (overwrite) keyed on `ticketId`, so re-saving the same ticket never creates a duplicate file — satisfies the Idempotency & Replayability contract. `ticketId` is validated against a safe-filename character set (`^[A-Za-z0-9_-]+$`) before it is ever used to build a path, rejecting path-traversal-shaped input (`../../etc/passwd`, embedded slashes) rather than interpolating it — the concrete handling for "Summary not savable" plus the Security Enforcement Layer's input-validation rule applied at the one place this story touches the filesystem. A blocked/failed write is caught, fails closed with `SaveAccessDeniedError`/`SaveFailedError`, and alerts to stderr (no external alerting system exists yet, same as `auditLog.js`) rather than throwing. Added `summaries/` to `.gitignore` (runtime ticket data, not shipped code — same reasoning as `*.log` for the audit trail). Extended `src/auditedActions.js` with `generateSupportSummaryAndLog(workflow, options?)` and `saveSupportSummaryAndLog(summary, options?)`, the same thin caller-side wrapper shape as the six existing actions, persisting each logEntry via `auditLog.js`'s existing hash-chained, fail-closed writer — this is what makes "Trust: the summary generation should be logged in the audit trail" true in practice. Added `tests/generateSupportSummary.test.js` (22 tests: both happy paths — resolved without escalation, resolved with an approved escalation — graceful `kbSearchResult`-absent degradation, every fail-closed validation branch, all three escalation-consistency mismatch cases, `logEntry` shape, the two `generateSupportSummaryAndLog` audit-persistence cases, purity/idempotency) and `tests/saveSupportSummary.test.js` (14 tests: happy-path save, nested-directory auto-creation, the idempotent-overwrite check, every fail-closed validation branch including the unsafe-ticket-id/path-traversal cases, a simulated blocked-write failure via the same-named-file technique `auditLog.test.js` already established, the two `saveSupportSummaryAndLog` audit-persistence cases, `logEntry` shape).
  - Verification: `npm test` — 179/179 pass (36 new across the two files, itemized above); the other 143 (classify + guardrail + presentToAgent + reviewClassification + auditLog + auditedActions + knowledgeBaseSearch + generateDraftResponse + generateEscalationRecommendation + reviewEscalation) unaffected. Re-ran the full suite 3 times in a row — 179/179 all three times, no flakiness (each new test file uses its own unique `tests/tmp/<name>-<uuid>/` subdirectory, same fix STORY-003 already established for the concurrent-file-runner race). `node --check` passed on `generateSupportSummary.js`, `saveSupportSummary.js`, and the modified `auditedActions.js`.
  - Notes: This is the last story in the plan per the story brief. No downstream ticketing-system write exists in this repo (same gap already documented for STORY-006's escalation approval) — `saveSupportSummary` writes only to the local `summaries/` directory, which is the correct stopping point for "can be saved with the ticket" absent a real ticketing integration; wiring an actual ticket-system attachment is future scope, not silently built here. `.colaberry/plan.json`/`.colaberry/progress.json` left untouched for the same reason documented in STORY-004/005/006 — the platform owns that sync and fills `verification` after the commit lands. Built as a paced co-pilot session per the story's instruction — design proposal, `generateSupportSummary.js`, its tests, `saveSupportSummary.js`, its tests, the `auditedActions.js` wiring, and this update were each proposed and confirmed as a separate step, nothing batched. No secrets introduced; `tsc --noEmit` does not apply (plain JS `src/` prototype tree, not the TypeScript backend/frontend layers described elsewhere in CLAUDE.md).

## Demo Preparation

- [x] Correct the record on demo status — no recording exists yet despite a commit message implying one does
  - Date: 2026-08-30
  - Session: CC-20260830-k3v9
  - What changed: Three commits from 2026-08-24 to 2026-08-25 were never logged in PROGRESS.md, so this file was silent on them: `6c0b620` ("PREP-1: add demo narrative and live demo script") and `8aac5ba` ("PREP-3: add demo slides") are real — they added `demo.js` (a runnable script wiring the tested `classifySupportRequest` → `searchKnowledgeBase` → `generateDraftResponse` modules together against one fake ticket) plus `docs/demo-narrative.md`, and `demo prep/support-workflow-assistant.pptx`, respectively. `f29a5e0` ("PREP-2: record and review first run-through demo") is **not** — the commit itself changes 0 files, and no video or audio recording exists anywhere in this repo. This entry corrects PROGRESS.md's record going forward; it does not alter the `f29a5e0` commit or its message, per the project's git-history-integrity rule (correcting the record, not the history).
  - Verification: `git show --stat f29a5e0` — 0 files changed. Repo-wide search for `*.mp4`/`*.mov`/`*.webm`/`*.avi`/`*.mkv`/`*.gif` — 0 results. `demo.js` confirmed present and runnable, wiring three real `src/` modules already covered by the 179-test suite. `demo prep/support-workflow-assistant.pptx` confirmed present (56,163 bytes).
  - Notes: **Follow-up planned, dated 2026-08-30:** an actual screen recording of `demo.js` / the live demo has not yet been made. When one is made, add the file (or a link, if hosted externally) here and update this entry — the PREP-2 commit message should not stand as the only claim that a recording exists. No code was changed by this correction.

## Claude Code Configuration — Commands, Hooks, CI

- [x] Add `/greet`, `/session-start`, `/progress-audit` custom slash commands; add a PreToolUse hook blocking manual writes to `/system`; add a GitHub Actions PR-review workflow
  - Date: 2026-08-27
  - Session: CC-20260827-n4qz
  - What changed: This is a catch-up entry covering Claude Code harness/config changes made across this session, logged together per the Catch-up rule since they weren't logged turn-by-turn. (1) `.claude/commands/greet.md` — a demo custom slash command outputting a personalized greeting from `$ARGUMENTS`. (2) `.claude/commands/session-start.md` — operationalizes the root CLAUDE.md "Session start protocol" (mint a `CC-<date>-<id>` Session ID, read CLAUDE.md + PROGRESS.md fully, summarize state, no edits). (3) `.claude/commands/progress-audit.md` — operationalizes the "End-of-session audit" hard gate (diff files changed this session against PROGRESS.md entries tagged with the active Session ID, write any missing entries with verification evidence, report a clean/dirty audit line). (4) `.claude/hooks/block-system-writes.cjs` + `.claude/settings.json` — a `PreToolUse` hook on `Edit|Write|NotebookEdit` that denies any write whose path falls under the top-level `/system` directory, per root CLAUDE.md's "Folder Responsibilities" rule that `/system` is portal-owned/auto-generated and "DO NOT manually edit." Written as `.cjs` (not `.js`) because this repo's `package.json` sets `"type": "module"`, which would otherwise make `require()` fail. (5) `.github/workflows/claude-code-review.yml` — a GitHub Actions workflow using the official `anthropics/claude-code-action@v1` with the `code-review@claude-code-plugins` plugin, triggered on `pull_request: [opened, synchronize, ready_for_review, reopened]`, posting inline review comments via `--comment`, with `concurrency` (cancel superseded runs per PR) and a 20-minute `timeout-minutes` added beyond the documented baseline example as cost/runaway-job guardrails. Requires the user to install the Claude GitHub App and add an `ANTHROPIC_API_KEY` secret to each target GitHub repo before it will run (not done by Claude Code — outside its access) — confirmed with the user beforehand which engine (Claude Code Action vs. static-analysis-only) and trigger scope (every PR vs. label-gated) to use, given the paid-external-service and CI/CD-pipeline-modification considerations in CLAUDE.md's Autonomy Model.
  - Verification: The two commands are plain markdown skill files (no runtime to test directly); reviewed by re-reading each against the exact CLAUDE.md protocol text it operationalizes. The hook was pipe-tested standalone (`echo '{...}' | node .claude/hooks/block-system-writes.cjs`) against a `/system` write (denied, correct JSON), a normal backend edit (silent allow), a `Read` tool call (silent allow), and a similarly-named-but-different file `system-notes.md` (silent allow, no false positive) — then proven live end-to-end by attempting a real `Write` to `system/intelligence/state_graph.json` through the harness itself, which the hook denied with its exact message; confirmed no stray file/directory was left behind. `.claude/settings.json` schema validated via a Node script reading the JSON and locating the hook's `command` field (`jq` was not available on this machine). The GitHub Actions YAML's exact structure (action ref, inputs, permissions block) was verified against the live, verbatim-fetched content of `https://code.claude.com/docs/en/github-actions.md` (not from a summarized/paraphrased fetch, which was caught giving an incorrect action name on a first attempt and discarded) — not executed, since that requires pushing to GitHub with the app installed and a real secret, which is outside this session's access.
  - Notes: The `/system` hook blocks the entire `/system` subtree, including a future `system/CLAUDE.md` itself — flagged to the user as a caveat, since the DRI may need a narrow carve-out for that one file later. The GitHub Actions workflow is untested end-to-end (no push performed, per this session's git-safety default of not pushing without explicit instruction) — the user still needs to install the Claude GitHub App and add `ANTHROPIC_API_KEY` on both `origin` (architect-workspace) and `assistant` (ai-support-workflow-assistant) remotes before the first PR will actually trigger a review.

- [x] Add `.github/workflows/test.yml` — run `npm test` on every pull request, as the highest-priority gap closed from the verification-mechanisms audit
  - Date: 2026-08-27
  - Session: CC-20260827-n4qz
  - What changed: Added `.github/workflows/test.yml`, triggered on every `pull_request` event, running `actions/checkout@v6` -> `actions/setup-node@v7` (Node 24, matching the version actually used locally this session) -> `npm install` -> `npm test`. No `package-lock.json` exists in this repo, so `npm ci` (which requires one) was not used; `npm install` was used instead, matching current repo state rather than silently adding a lockfile as an unrequested scope expansion. `package.json` has zero runtime/dev dependencies (the suite runs entirely on Node's built-in `node --test`), so the install step is a no-op today but is included for when dependencies are added later. This directly answers gap #3 from this session's verification-mechanisms audit (no CI gate ran the 179-test suite; all passes were self-reported from local runs). The workflow only runs tests — it does not itself enforce "can't merge on failure"; that requires a GitHub branch protection rule (Settings -> Branches -> require status check "test"), which is a live repository-settings change outside what Claude Code can perform, so it was handed to the user as a plain-language walkthrough instead of executed via `gh api`.
  - Verification: `actions/setup-node@v7` was confirmed as the actual current major version via a live fetch of the action's GitHub releases page (not assumed from training knowledge, since `actions/checkout@v6` earlier this session had already moved past the version I'd have otherwise guessed). YAML indentation verified by reading the file back (2-space, consistent, no tabs) — no local YAML linter available on this machine to parse-check it programmatically. Not executed, since running it requires pushing to GitHub; no push was performed.
  - Notes: The workflow itself is a required-check candidate but is NOT yet enforced as one — enforcement is a manual GitHub Settings step the user still needs to do per-repo (on both `origin` and `assistant`), documented for them in the same turn as plain-language instructions rather than performed via `gh api`, since branch-protection changes affect shared repo state (who can merge what) and this session has no confirmation of what protection rules, if any, already exist on either remote.

- [x] Add `/mark-verified` slash command and a `check-colaberry-schema` PostToolUse hook
  - Date: 2026-08-27
  - Session: CC-20260827-w5j2
  - What changed: Asked the user two questions about this repo's recurring friction — what they do over and over (marking a STORY's acceptance criteria verified in `.colaberry/progress.json`, the pattern behind commits `aa50ebc9`/`7b3fc504`/`5968beb2`) and what they forget until it surfaces later (keeping `.colaberry/plan.json` and `progress.json` in sync, the exact drift class `5968beb2` had to repair after it broke the Command Center in `2fedfc8`) — then built one of each, matching command-vs-hook to which answer it came from. Added `.claude/commands/mark-verified.md`: a slash command (`/mark-verified STORY-ID [sha]`) that proceduralizes the verified-marking pattern — cross-check against `plan.json`'s `acceptance` array, confirm criteria actually pass before marking them, derive `commit_sha`/`commit_url`/`commit_at` from the real `git remote`/`HEAD` (not a hardcoded repo URL), recompute `progress.json`'s `totals` block from `stories[]` rather than hand-editing individual numbers, and remind the PROGRESS.md hard gate. Added `.claude/hooks/check-colaberry-schema.cjs`, registered as a `PostToolUse` hook (matcher `Edit|Write|NotebookEdit`) in `.claude/settings.json` alongside the existing `block-system-writes.cjs` `PreToolUse` hook: after any edit to `.colaberry/plan.json` or `progress.json`, it re-reads both from disk and blocks (via `{"decision":"block","reason":...}`) if a story `plan.json` expects is missing from `progress.json`, a story's `criteria[]` count/text disagrees with `plan.json`'s `acceptance[]`, a `verification` block's `criteria_total`/`criteria_passed` disagree with the actual `criteria[]` array, a story is marked `"verified"` with unmet criteria or non-empty `outstanding`, or the top-level `totals` block's arithmetic disagrees with what `stories[]` implies.
  - Verification: Pipe-tested the hook standalone against the live repo. Against the current (consistent) `.colaberry/*.json` state it initially reported a false positive — `STORY-000` present in `progress.json` with no match in `plan.json` — which on inspection was legitimate (STORY-000 is a Command Center meta-story tracked only in `progress.json` + `docs/stories/STORY-000.md`, per `manifest.json`), not drift; narrowed the hook's story-id check to only the dangerous direction (a `plan.json` story missing from `progress.json`) and re-ran — silent/clean exit on the real current state. Then reproduced the exact bug class fixed in `5968beb2` (set `stories[1].verification.points_awarded` to `null` on a scratch copy, swapped it in) and confirmed the hook correctly blocked with `totals.points_awarded is 800 but the stories[] array implies 700`; restored the original file from a backup immediately after (`git status --short` confirmed clean, only the three intended files touched). The command is a markdown prompt file with no runtime to execute directly — reviewed by re-reading it against the exact update pattern present in the STORY-00x commit history and against `progress.json`'s real schema (`totals`, per-story `criteria`/`verification` shape) read directly from the file.
  - Notes: The hook is intentionally asymmetric (plan→progress only, not progress→plan) because of the STORY-000 finding above — flagged to the user in the same turn it was found, not silently patched away. No code in `/backend`/`/frontend` touched, so no `tsc --noEmit` gate applies; this is Claude Code harness configuration (`.claude/`), same category as the prior two entries in this section.

## Architecture Documentation

- [x] Add `architecture/7-layer-architecture-mapping.md` — 7-layer reference architecture mapped to the AI Support Workflow Assistant
  - Date: 2026-08-30
  - Session: CC-20260830-z2q6
  - What changed: Added `architecture/7-layer-architecture-mapping.md`, mapping the general 7-layer AI reference architecture (Infrastructure, Data, Model, Orchestration/Reasoning, Application/Interaction, Governance & Trust, Observability & Feedback) onto this project's real components, cross-referencing both `project-blueprint/architecture.md` (design intent) and this file's own build history (actual implementation). Each layer section cites real files (`src/classify.js`, `src/guardrail.js`, `src/auditLog.js`, `src/auditedActions.js`, etc.) and real tests (`tests/auditLog.test.js`, `tests/guardrail.test.js`, `tests/reviewClassification.test.js`) rather than generic descriptions, and every claim is tagged Confirmed/Documented/Gap by evidence tier. Surfaces two corrections to the conceptual picture in `architecture.md`: no Support Request Store exists as a built module (Layer 2), and no Support Request Entry / Human Review UI exists yet (Layer 5) — both are function-argument-level in the current tests, not a live surface. This is a documentation-only change; no `src/`, `tests/`, or `.colaberry/*.json` files were modified.
  - Verification: Every file and test citation in the document was checked against the actual repository before being written — `src/*.js` file list confirmed via directory listing, `classify.js`/`auditLog.js`/`generateDraftResponse.js`/`generateSupportSummary.js` read in full, `guardrail.js`/`generateEscalationRecommendation.js` confirmed via direct comment reads, and the specific test names quoted (tamper-detection, missing-reviewer rejection, `RESTRICTED_ACTIONS` iteration) confirmed by reading the relevant test files directly rather than inferred from `PROGRESS.md` summaries alone. No code was changed, so no `npm test`/`tsc --noEmit` gate applies; this is an in-repo doc per CLAUDE.md's Logging section, so it still requires this PROGRESS.md entry.
  - Notes: This session had no prior repo-touching work, so this is the first PROGRESS.md entry under session `CC-20260830-z2q6`, not a catch-up entry. `architecture.md` (Student Support Workflow Assistant) and this file's own title (AI Support Workflow Assistant) use slightly different project names for the same system — carried through as-is rather than silently reconciled, since renaming either existing doc was out of scope for this change.

## Accelerator Capstone Deliverables

- [x] Build `AISolutionArchitect_FieldGuide.html` — Week 12 AI Solution Architect capstone field guide
  - Date: 2026-08-30
  - Session: CC-20260830-t7m3
  - What changed: Added `AISolutionArchitect_FieldGuide.html` at the repo root — a single self-contained, offline-capable HTML knowledge base for the Colaberry Enterprise AI Leadership Accelerator's Week 12 (AI Solution Architect) capstone. It is not part of the AI Support Workflow Assistant product tracked elsewhere in this file; it is a standalone curriculum artifact. Contents: (1) a left topic nav, a full-text search box, and an offline "Ask the guide" assistant (curated Q&A matched by keyword overlap, falling back to a full-text section match — no external API, all logic runs client-side against the page's own DOM content); (2) eleven concise "Foundations" teaching sections covering the architect's synthesis role, end-to-end traceability, ADRs, whole-system trade-offs, the architecture package as SSOT, C4 views/NFRs, cost/roadmap method, presenting a design, good-vs-bad architecture, post-launch KPIs, and a reviewer's checklist; (3) a worked case study — "ClaimSight AI," an AI-assisted FNOL intake/damage-estimation/fraud-triage initiative for a fictional regional P&C insurer (Meridian Mutual Insurance) — referencing invented upstream artifacts from all eleven accelerator disciplines; (4) six full deliverable documents, each rendered inline and individually downloadable as a branded standalone HTML file (cover with the real Colaberry logo, document-control strip, sign-off block, footer) and printable to PDF via a new-window `window.print()` flow, with CSV export for the four tabular ones: the Solution Architecture Document (C4 context/container diagrams, a sequence diagram, an ERD, and a deployment/CI-CD diagram, all as hand-authored inline SVG, plus integration/AI/security/NFR/risk views), the Architecture Decision Log (8 ADRs, each with 3 options and a stated winner), the Document-Set Index & Traceability (11-discipline artifact index plus a 14-row requirement→design→build→test→run→govern table), the Cost Model (build/run cost tables with inline SVG bar charts, unit economics, and a 12-month TCO table), the Delivery Roadmap (phase table plus an inline SVG Gantt timeline with 5 approval gates), and the Capstone Package with a sponsor-facing one-page Executive Summary (KPI tiles). Branding follows the exact Colaberry spec from root `CLAUDE.md`'s UI/UX section: the real logo fetched from `https://enterprise.colaberry.ai/colaberry-logo-transparent.png` and embedded as an inline base64 data URI (no drawn/substitute logo), the exact five-color palette plus dark-mode variants, Roboto/Roboto Mono with system-ui/Arial fallback. Embeds a `deepdive-metadata` JSON script tag (guide_type, curriculum_type, week 12, discipline, ids, generator, dates, version). Entirely self-contained except the Google Fonts stylesheet link (CSS/JS/logo all inline); opened in the default browser at the end of the session per the request.
  - Verification: Programmatic post-build validation (Python) confirmed: open/close tag counts balance for section/article/div/main/nav/header/footer/table/svg (21/6/331/1/1/1/1/22/10 respectively, all matched); all 21 sidebar nav `href="#..."` targets resolve to an existing element `id`; the `deepdive-metadata` script tag contains valid, parseable JSON; no leftover template placeholders (`PLACEHOLDER_LOGO_BASE64`, `__LOGO__`) remain after the logo-substitution pass; the embedded logo is the actual fetched PNG (291×82, ~19KB) base64-encoded, not a generated substitute. Not run through a browser-automation check in this session — "user confirmed" visual verification is pending; the file was opened via `start` for the user to inspect.
  - Notes: This is a catch-up entry — the session proceeded directly to building the deliverable (per Auto Mode's proceed-by-default bias on a self-contained, low-blast-radius content-generation task) without running the formal Session Start Protocol first; logged in full here per the Catch-up rule rather than left unlogged. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched, so no `tsc --noEmit` gate applies. Scratch files `logo_tmp.png` and `logo_b64.txt` (used only to stage the base64 conversion) were deleted from the repo root after the embed succeeded, consistent with this project's scratch-space convention — they were never committed.

## Standalone Scratch Scripts

- [x] Add `hello_claude.py` — minimal Anthropic SDK smoke-test script
  - Date: 2026-08-31
  - Session: CC-20260831-ytlb
  - What changed: Added `hello_claude.py` at the repo root — a ~15-line standalone script (not part of `src/`, `/backend`, or `/frontend`) that calls `client.messages.create()` against `claude-opus-5` with a support-ops system prompt and one fixed user question ("A customer cannot log in after a password reset. What do you need to help?"), prints the reply text, and prints input/output token counts from `response.usage`. Uses `anthropic.Anthropic()` with no arguments (reads `ANTHROPIC_API_KEY` from the environment, never hardcoded/printed) and calls `load_dotenv()` at the top (user-requested addition) so a local `.env` is picked up. `max_tokens` was raised from the originally requested 512 to 1024 after the first two live runs showed `claude-opus-5`'s extended thinking (enabled by default) shares the same token budget as the reply — one run consumed the entire 512-token budget on thinking alone (`stop_reason: max_tokens`, no text block at all). Flagged this deviation from the explicit user-specified value to the user directly (not silently changed) and let them pick the fix; they chose raising `max_tokens` over disabling thinking or handling the empty-reply case in code. Reply-text extraction uses `next(block.text for block in response.content if block.type == "text")` instead of `response.content[0].text`, since thinking responses return a `ThinkingBlock` before the `TextBlock` and indexing `[0]` crashed on the first run (`AttributeError: 'ThinkingBlock' object has no attribute 'text'`).
  - Verification: Ran `python hello_claude.py` after the `max_tokens` fix — completed successfully, printed a full multi-section support-ops reply and `Input tokens: 44 | Output tokens: 598` on the final line.
  - Notes: This is a catch-up entry — this session did not run the formal Session Start Protocol before starting the work (the request was small/interactive); Session ID `CC-20260831-ytlb` was minted at this entry per the Catch-up rule. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched, so no `tsc --noEmit` gate applies to this plain-Python root script.

- [x] Add `triage.py` — structured-JSON support-ticket triage using Messages API `output_config`/`json_schema`
  - Date: 2026-08-31
  - Session: CC-20260831-ytlb
  - What changed: Added `triage.py` at the repo root, based on `hello_claude.py`'s client setup (`anthropic.Anthropic()` with no arguments, `load_dotenv()` called first, key never hardcoded or printed). Sends one fixed support ticket ("Where is my order ORD-4471? It has been two weeks!") to `claude-opus-5` with `output_config: {format: {type: "json_schema", schema: {...}}}` constraining the reply to exactly four fields — `category` (enum: shipping/billing/technical/other), `urgency` (enum: low/normal/high), `order_id` (string, empty if not mentioned), `suggested_reply` (string) — with `additionalProperties: false` and all four in `required`. Per the `claude-api` skill's documented `json_schema` pattern, `output_config.format` guarantees the first content block is `text` containing valid JSON, so the script parses it directly via `json.loads(next(block.text for block in response.content if block.type == "text"))` rather than the `content[0]`-indexing approach that broke on `hello_claude.py`'s first run. Prints each of the four fields on its own labelled line, then input/output token counts, matching `hello_claude.py`'s usage-line format.
  - Verification: Ran `python triage.py` — succeeded on the first run with valid structured JSON (`category: shipping`, `urgency: high`, `order_id: ORD-4471`, a full `suggested_reply` string) and `Input tokens: 356 | Output tokens: 200`; no `max_tokens`/thinking-block issue recurred, consistent with the skill's documented guarantee that `output_config.format` puts a text block first.
  - Notes: Continuation of session `CC-20260831-ytlb` (see the `hello_claude.py` entry above). No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched, so no `tsc --noEmit` gate applies to this plain-Python root script.

- [x] Add `orders.json` + `assistant.py` — full tool-use round trip (order lookup) demo
  - Date: 2026-08-31
  - Session: CC-20260831-ytlb
  - What changed: Added `orders.json` at the repo root — a 5-record order database (`order_id`/`customer_name`/`status`/`carrier`/`eta`), `ORD-4471` deliberately set to `status: "delayed"` per the request so the demo has something interesting to report; the other 4 (`ORD-4472`-`ORD-4475`) cover `in_transit`/`delivered`/`lost`. Added `assistant.py`, based on `triage.py`'s client setup (`anthropic.Anthropic()` no-arg, `load_dotenv()` first, key never hardcoded/printed). Defines one custom tool, `lookup_order` (required string `order_id` in `input_schema`), implemented in Python by reading `orders.json` and returning a JSON string of `{status, carrier, eta}` on match or a plain not-found message otherwise. Sends the ticket "Hi - where is order ORD-4471??..." to `claude-opus-5` with the tool available; handles the full manual agentic-loop round trip per the `claude-api` skill's documented pattern (not the beta tool runner, since the request asked for explicit round-trip handling): checks `response.stop_reason == "tool_use"`, extracts the `tool_use` block, runs the real `lookup_order()` function, appends the assistant turn (`response.content` verbatim, not just the text), sends back a `tool_result` content block matching `tool_use.id`, and calls the API a second time so Claude can produce the final reply. Prints, clearly labelled: the tool name and arguments Claude requested, what the Python function actually returned, and the final customer-facing reply; accumulates and prints `input_tokens`/`output_tokens` summed across both API calls (not just the second one).
  - Verification: Ran `python assistant.py` — succeeded on the first run. Output showed `Tool requested: lookup_order`, `Tool arguments: {'order_id': 'ORD-4471'}`, `Tool result: {"status": "delayed", "carrier": "UPS", "eta": "2026-09-03"}` (matching `orders.json` exactly), a coherent final reply grounded in that real tool output (correctly stated as delayed, did not fabricate a reason for the delay), and `Total input tokens: 983 | Total output tokens: 462` summed across both calls.
  - Notes: The printed final reply displayed `�` in place of em-dashes in this session's terminal — a Windows console codepage rendering artifact (cp1252 vs. the UTF-8 text Claude returned), not a bug in `assistant.py`, `orders.json`, or the API response; flagged rather than silently touched, no fix applied since nothing is actually broken. Continuation of session `CC-20260831-ytlb`. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched, so no `tsc --noEmit` gate applies to these plain-Python/JSON root files.

- [x] Add `cost.py` — pure-arithmetic per-model cost comparison (no API call)
  - Date: 2026-08-31
  - Session: CC-20260831-ytlb
  - What changed: Added `cost.py` at the repo root, defining `price(input_tokens, output_tokens, model)` against a `RATES` dict of per-million-token input/output rates for `claude-opus-5` ($5.00/$25.00), `claude-sonnet-5` ($3.00/$15.00), and `claude-haiku-4-5` ($1.00/$5.00). Makes no API call — pure arithmetic. Asked the user which token counts to use for the comparison table (this session's `assistant.py` total of 983 input / 462 output vs. `triage.py`'s 356/200 vs. a different pasted number) since the request said counts would be "pasted in" but none arrived in that message; user chose the `assistant.py` total. Prints a 3-row table (one per model) showing cost for that one call formatted to 5 decimal places, plus what 1,000 calls/day would cost per month (call cost × 1,000 × a 30-day-month approximation, flagged as an assumption via a code comment rather than silently picking 30/31/28 without disclosure).
  - Verification: Ran `python cost.py` — printed a clean aligned table: `claude-opus-5` $0.01647/call, $493.95/month; `claude-sonnet-5` $0.00988/call, $296.37/month; `claude-haiku-4-5` $0.00329/call, $98.79/month. Spot-checked the opus row by hand: (983/1e6)*5 + (462/1e6)*25 = 0.004915 + 0.01155 = 0.016465, rounds to 0.01647 — matches.
  - Notes: Continuation of session `CC-20260831-ytlb`. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched, so no `tsc --noEmit` gate applies to this plain-Python root script.

- [x] Build `assistant/` package — LLM-powered support-ticket triage workflow (classify → search KB → draft/escalate → human approval)
  - Date: 2026-08-31
  - Session: CC-20260831-ytlb
  - What changed: Planned in Plan Mode first (plan approved before writing any code; saved at `C:\Users\farha\.claude\plans\declarative-coalescing-stream.md`). Added the `assistant/` package: `client.py` (configured `anthropic.Anthropic()` singleton, centralized `MODEL = "claude-opus-5"` constant, `load_dotenv()` called once — fixes the repeated-literal/repeated-load_dotenv pattern across this session's four root scripts), `prompt.py` (`SYSTEM_PROMPT` stating the 7 categories/4 priorities and the plain-language safety boundary), `tools.py` (`search_knowledge_base` tool reading the **existing** `src/data/knowledgeBase.json` — reused, not duplicated — with category+tag scoring mirroring `src/knowledgeBaseSearch.js`'s weighting), `core.py` (`handle_ticket()`, a `while stop_reason == "tool_use"` round-trip loop combining `tools` + `output_config` structured JSON in the same calls, token accumulation across every call in the loop, and the load-bearing safety decision: `requires_human_approval = True` / `status = "proposed"` are Python-hardcoded literals appended after parsing the model's JSON, never sourced from the model's own output — mirrors `src/guardrail.js`'s discipline in code rather than trusting a prompt instruction alone), and `evals.py` + `eval_cases.jsonl` (7 cases, one per KB-backed category plus one escalation-path case, grading reused from `scripts/score_prompt.py`'s tolerant field-matching philosophy plus a safety-specific check: `requires_human_approval`/`status` presence and a forbidden-claim-of-action-phrase scan on every `draft_reply`). Exactly 5 responsibility files as requested, each with the plan's documented "must never" boundaries (e.g. `tools.py` is read-only by construction — no write access to the KB file; `core.py` performs no actual send/close/escalate side effect).
  - Verification: Live smoke test (`python -c "from assistant import handle_ticket; ..."`) surfaced a real bug on the first run: the model returned the `keywords` tool argument as a single comma-separated **string** instead of the declared JSON array, and the scoring loop then iterated over individual characters, producing a nonsensical relevance score of 67. Fixed by adding `"strict": True` (+ explicit `additionalProperties: False`) to `SEARCH_KB_TOOL` and, independently, hardening `search_knowledge_base()` to defensively normalize non-list `keywords` input rather than trust the declared schema — re-ran the same smoke test, got a correct array argument and a sane score (5). Then ran the full eval harness (`python -m assistant.evals`) twice: the first run scored 3/7 and the four "failures" were the assistant correctly recommending escalation for permission changes (KB-003: "a human must approve and apply it"), a DBA-level deadlock (KB-005: "not a self-serve fix"), and an undocumented topic (KB-007: "note this as a documentation gap") — my original eval fixtures had wrongly assumed "KB match found" implied "no escalation needed." Corrected `eval_cases.jsonl`'s expected `escalation_recommended` values to match what the KB articles' own text actually calls for (not to force a target answer), re-ran, got 7/7 (score 1.00), with every case also passing the safety-boundary assertions (`requires_human_approval == True`, `status == "proposed"`, no forbidden claim-of-action phrases in any draft reply).
  - Notes: This is a separate, LLM-powered Python implementation of the same conceptual workflow the deterministic JS prototype under `src/` already has fully built and verified (STORY-001 through STORY-007, 26/26 criteria, per `.colaberry/progress.json`) — per CLAUDE.md's core principle that production classification should be deterministic, this new package is explicitly a demonstration/learning track, not a replacement for the JS implementation. A `guardrail.py`/`presentToAgent.py`-equivalent validator and audit-trail persistence were deliberately left out of scope (the user asked for exactly 5 files, not 6) — flagged in the plan as a natural next step rather than silently added. Continuation of session `CC-20260831-ytlb`. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched, so no `tsc --noEmit` gate applies to this plain-Python package.

- [x] Add `MAX_TOOL_TURNS` guard to `assistant/core.py`'s tool-use loop
  - Date: 2026-08-31
  - Session: CC-20260831-q7f2
  - What changed: The `while response.stop_reason == "tool_use":` loop in `handle_ticket()` (`assistant/core.py`) had no cap on iterations — a model that kept requesting tools would loop forever, violating CLAUDE.md's Stall Detection rule ("infinite retry loops are explicitly prohibited") and the Failure-First Design external-boundary rule (every outbound call needs a bounded retry strategy). Added `MAX_TOOL_TURNS = 5` as a module constant; the loop now increments a `tool_turns` counter each pass and breaks with `loop_limit_exceeded = True` once it exceeds the cap, instead of calling the model again. When the limit trips, `handle_ticket()` skips parsing a final-answer text block (there isn't one) and returns a hardcoded safe fallback conforming to `OUTPUT_SCHEMA`: `category: "general_support_request"`, `confidence: "none"`, `escalation_recommended: true`, and an `escalation_reason` naming the exceeded limit — then goes through the same `requires_human_approval`/`status`/token-accounting tail as the normal path, so callers don't need to special-case it.
  - Verification: No committed test file was added (this package was scoped to exactly 5 files per the prior entry's plan; a guard fix doesn't reopen that scope). Verified instead with a scratch script (`verify_max_tool_turns.py`, run from the temp scratchpad, not committed) that monkeypatches `client_module.client.messages.create` to always return a `stop_reason: "tool_use"` response, simulating a model that never stops calling tools: confirmed exactly 6 API calls were made (1 initial + `MAX_TOOL_TURNS` turns, matching the constant exactly, not off-by-one), the loop terminated instead of hanging, and the returned dict has `escalation_recommended is True`, `confidence == "none"`, `requires_human_approval is True`, `status == "proposed"`, and `"MAX_TOOL_TURNS"` present in `escalation_reason`. `python -c "import ast; ast.parse(open('assistant/core.py').read())"`-equivalent check: file re-read and confirmed to parse/import cleanly (the verification script itself imports `assistant.core` successfully).
  - Notes: Chose a hardcoded fallback proposal over re-raising or returning partial/`None` data, matching this module's existing pattern of never trusting the model's own output for the safety-relevant fields (`requires_human_approval`/`status` are already Python-hardcoded literals, not model-sourced, per the prior entry). `category: "general_support_request"` was picked as the fallback since it's the KB's own documented catch-all category (no KB article backs it, consistent with `src/data/knowledgeBase.json`'s existing convention). Did not run `python -m assistant.evals` (the 7-case real-API eval harness) for this change since it only exercises the happy/known-category paths and would not hit the loop-limit branch. New session ID minted (`CC-20260831-q7f2`) since this conversation did not run the Session Start Protocol at its outset (opened directly on a file-read request) — logged per the Catch-up rule rather than reusing today's other session ID (`CC-20260831-ytlb`).

- [x] Add a final structured-output step to root `assistant.py` (order-status tool-use demo)
  - Date: 2026-08-31
  - Session: CC-20260831-q7f2
  - What changed: `assistant.py` (the root-level `lookup_order` tool-use demo -- the file that actually exists in this repo; the `assistant/` package is a separate, already-built 5-file triage system and has no `assistant.py` inside it, so this request was applied to the root script, flagged rather than silently reinterpreted) previously ended once it had a free-text `final_reply`. Added a third API call, made only after the tool round trip resolves, using `output_config: {"format": {"type": "json_schema", "schema": ORDER_STATUS_SCHEMA}}` -- the current, non-deprecated structured-output mechanism (not the deprecated top-level `output_format` parameter) -- reusing the same pattern already established in `assistant/core.py`/`triage.py`. Designed `ORDER_STATUS_SCHEMA` for this specific ticket workflow: `resolution_category` (enum: `on_time`/`delayed`/`lost`/`order_not_found` -- the decision), `order_facts` (nested object: `order_id`/`status`/`carrier`/`eta`, all required, `additionalProperties: false` -- the facts `lookup_order` actually returned, with a schema description telling the model never to invent them), `urgency` (enum: `low`/`medium`/`high`), and `customer_reply` (string -- the human-readable output). Top level and the nested `order_facts` object both set `additionalProperties: false` with every field in `required`, per the request. Added `get_structured_result(messages, total_input_tokens, total_output_tokens) -> dict`: makes the call, extracts the text block, `json.loads`s it, and on `json.JSONDecodeError` raises a new named `StructuredOutputError` (wrapping the original exception via `raise ... from e`) instead of returning a partially-built dict -- satisfies "raise a clear named error instead of returning a half-built record." On success it returns the parsed dict with `total_input_tokens`/`total_output_tokens` merged in (the running totals plus this call's own usage), matching "return it as a plain dict together with the accumulated token totals."
  - Verification: Ran `python assistant.py` twice. First run hit a real bug: appending `{"role": "assistant", "content": response.content}` before the third call left the conversation ending on an assistant turn, and the API rejected it with `400 invalid_request_error: This model does not support assistant message prefill. The conversation must end with a user message.` Root cause: `messages` already ended on a `user` turn (the `tool_result` block) after the tool round trip -- no new turn needed to be appended at all. Removed the erroneous append (comment left explaining why) and re-ran: succeeded cleanly, printing a schema-valid structured result -- `resolution_category: "delayed"`, `order_facts` exactly matching `orders.json`'s real `ORD-4471` record (`status: "delayed"`, `carrier: "UPS"`, `eta: "2026-09-03"`, no fabricated facts), `urgency: "high"`, a full `customer_reply` string, and `total_input_tokens: 1760 | total_output_tokens: 729` (correctly summed across all three API calls, verified higher than the pre-existing two-call total would have been).
  - Notes: No committed test file was added, consistent with this script's existing style (a runnable root demo, not a package with a test suite) -- verified via a live run against the real API and real `orders.json` data instead. The `StructuredOutputError` path (malformed JSON from the model) was not forced/exercised directly in this session, since `output_config`'s `json_schema` format is documented to guarantee a parseable text block in normal operation; the `try/except` exists as the fail-closed guard for the boundary regardless. Continuation of session `CC-20260831-q7f2`. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched, so no `tsc --noEmit` gate applies to this plain-Python root script.

- [x] Add `assistant/assistant.py` -- CLI entry point for the `assistant/` package, run once on a real workflow ticket
  - Date: 2026-08-31
  - Session: CC-20260831-q7f2
  - What changed: The prior entry's assumption that `assistant/assistant.py` didn't exist and the request meant the root `assistant.py` was itself a misread -- this request (and the one before it, in hindsight) meant the `assistant/` *package* the whole time; "runs the full assistant" only makes sense against `assistant/core.py`'s 7-category triage workflow (`handle_ticket`), not the root single-tool order-lookup demo. Corrected course by creating the file at the literal path requested: `assistant/assistant.py`, a small CLI entry point (`python -m assistant.assistant "<ticket text>"`) with a `main()` guarded by `if __name__ == "__main__":`. It takes the ticket text as `sys.argv[1]` (usage message + `sys.exit(1)` on a missing argument, not a stack trace), calls `handle_ticket()` unchanged, then prints three clearly labelled sections: `=== TOOLS CALLED ===` (each call's tool name and arguments), `=== STRUCTURED RECORD ===` (the model's output JSON, tokens/tool_calls popped out first so they don't duplicate the dedicated sections), and `=== TOKEN USAGE ===` (the run's total input/output tokens). This required one small, additive change to `assistant/core.py` (`handle_ticket`) so the tool-call trace is actually available to a caller instead of only ever being printed inline: added a `tool_calls: list[dict]` accumulator, appended `{"name": tool_use.name, "arguments": tool_use.input}` for every tool call inside the existing loop (same place `cited_article_ids` is already tracked), and added `data["tool_calls"] = tool_calls` next to the existing `data["total_input_tokens"]`/`data["total_output_tokens"]` post-hoc fields (present on both the normal-completion and `loop_limit_exceeded` return paths, so the MAX_TOOL_TURNS guard added earlier this session still returns a valid, if partial, tool-call trace rather than omitting the key). Checked `evals.py`'s `case_passes()` before making this change -- it only iterates `expected.items()`, so adding an unrelated key to `handle_ticket`'s return dict cannot break the existing eval harness.
  - Verification: Ran `python -m assistant.assistant "My SQL query keeps timing out whenever I try to join more than two tables in the practice database. It worked fine last week. What should I check?"` (the user's real workflow item) once, live against the real API. Correctly classified `category: "sql_database_issue"`, called `search_knowledge_base` exactly once with `{"category": "sql_database_issue", "keywords": [...]}`, matched the real `KB-005` article (database timeout/deadlock), produced a grounded draft reply referencing that article's actual steps (no fabricated troubleshooting), returned `escalation_recommended: false` with a reasoned justification, and correctly carried `requires_human_approval: true` / `status: "proposed"`. All three labelled output sections rendered correctly: `TOOLS CALLED` showed the one real tool call with its real arguments, `STRUCTURED RECORD` showed the full record without a redundant `tool_calls`/token keys inside it, `TOKEN USAGE` showed `Total input tokens: 3313 | Total output tokens: 691` matching the sum printed by `core.py`'s own internal log line for the same run.
  - Notes: No committed test file was added for the same reason as the sibling root-`assistant.py` entry above -- this is a thin, directly-runnable CLI wrapper, verified by one real, observed, non-fabricated run rather than a mocked unit test. `python -m assistant.assistant` (not `python assistant/assistant.py`) is the required invocation, matching the relative-import convention `assistant/evals.py` already established and documented in this file's own prior entries. Continuation of session `CC-20260831-q7f2`. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched, so no `tsc --noEmit` gate applies to this plain-Python package.

- [x] Add `assistant/eval_set.json` -- 10-case draft eval set for `handle_ticket()`, expected values intentionally left unfilled pending user confirmation
  - Date: 2026-08-31
  - Session: CC-20260831-q7f2
  - What changed: Added `assistant/eval_set.json`, a new 10-case eval set for the `assistant/` package's ticket-triage workflow, distinct from and not replacing the existing 7-case `assistant/eval_cases.jsonl` (different filename/format -- one JSON array with metadata, not JSONL -- and a superset of graded fields: `category`, `priority`, `confidence`, `escalation_recommended`, `kb_articles_cited`, vs. the existing file's `category`/`escalation_recommended` only). Per explicit instruction, every case's `expected` block was written with all values set to `null` -- none of the answers were invented. 7 cases cover one ticket per `tools.CATEGORIES` value grounded in the user's real data/SQL-support workflow context (login lockout, new-role access request, Power BI refresh failure, a duplicate-records data issue, a SQL-export how-to question, plus a second SQL/indexing scenario since that's the core of the user's actual work). The 3 requested deliberately-awkward cases, each tagged with an `awkward_type` field: `EVAL-07` (`tool_finds_nothing` -- a vague general_support_request-shaped ticket; `general_support_request` has no article in `src/data/knowledgeBase.json` at all, and the wording was kept generic enough that no other category's tags should score a real match either), `EVAL-08` (`genuinely_ambiguous` -- "can't get into the finance Power BI workspace, not sure if my access got pulled or the report itself is broken," deliberately readable as either `access_permission_issue` or `power_bi_report_issue`), and `EVAL-09` (`malformed_or_incomplete` -- "it broke again pls fix," no system named, no error described, no category signal). `EVAL-04` reuses the user's actual real ticket from this session's earlier `python -m assistant.assistant` run verbatim, with a `note` field disclosing the observed historical result (`category='sql_database_issue'`, `confidence='high'`, `escalation_recommended=false`, `kb_articles_cited=['KB-005']`) without copying it into `expected` -- flagged as "confirm whether it's actually the right target," not assumed correct just because it's what the model happened to produce once.
  - Verification: `python -c "import json; json.load(open('assistant/eval_set.json'))"` -- valid JSON, confirmed 10 cases present. Not run through `evals.py` or any grading logic -- every `expected` value is `null`, so a grading run would be meaningless until the user fills them in; that confirmation step is the explicit next action, not done here.
  - Notes: File is explicitly `"status": "DRAFT - expected values not yet confirmed by the user"` at the top level, with an `instructions_for_reviewer` field restating what's being asked. This entry marks the *creation* of the draft as done, not the eval set itself as validated -- per this repo's rule against marking `[x]` based on intent, the "done" claim here is scoped only to "the draft file exists and is valid JSON with 10 well-formed, real-workflow-grounded cases," which is what was actually verified; grading/confirmation is a separate follow-up. Continuation of session `CC-20260831-q7f2`. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched, so no `tsc --noEmit` gate applies to this plain-JSON data file.

- [x] Confirm 2 of the 3 awkward `eval_set.json` cases (EVAL-07, EVAL-09); EVAL-08 blocked on a real discrepancy surfaced during confirmation
  - Date: 2026-08-31
  - Session: CC-20260831-q7f2
  - What changed: User supplied their own expected values (not invented by Claude) for all three deliberately-awkward cases. `EVAL-07` and `EVAL-09` were written in as given: `category: "general_support_request"`, `priority: "low"`, `confidence: "none"`, `escalation_recommended: true`, `kb_articles_cited: []` for both. For `EVAL-08` the user gave `category: "login_problem"`, `priority: "medium"`, `confidence: "low"`, `escalation_recommended: false`, and asked for `kb_articles_cited` to be "whatever the real KB search actually returns" rather than hand-picked. Ran the real pipeline against EVAL-08's exact ticket text (`handle_ticket()` called directly via `python -c`, live API call, after the CLI's first attempt hit a transient API error noted below) to get that real answer, and it does not match the rest of the user's stated expectation: the live run classified `category: "access_permission_issue"` (not `login_problem`), searched twice (`access_permission_issue` then `power_bi_report_issue`), cited `kb_articles_cited: ["KB-003", "KB-004"]`, returned `confidence: "medium"` and `escalation_recommended: true`. Independently confirmed this isn't a fluke of that one run's keyword choice: called `search_knowledge_base("login_problem")` directly (the category the user actually wants) with no keywords at all, and even a pure category-only match returns `confidence: "medium"` (KB-001 and KB-002 both score 3 on category match alone) -- there's no way for a `login_problem` search on this KB to land on the user's stated `confidence: "low"`. Left `EVAL-08`'s `expected` block as `null` rather than writing in either the user's un-matching values or the live-observed ones -- this is a real conflict between what the user wants and what the current KB/model produce, not an ambiguity Claude can resolve unilaterally.
  - Verification: `python -c "import json; json.load(open('assistant/eval_set.json'))"` -- still valid JSON after the two edits. The two live/direct-call checks against EVAL-08 (full pipeline once, direct `search_knowledge_base` call once) are shown above; not treated as "the answer" for EVAL-08, only as evidence of the conflict reported back to the user.
  - Notes: One transient, unrelated hiccup during this work: the first attempt to run EVAL-08 through the CLI (`python -m assistant.assistant "..."`) failed on the very first API call with a generic `400 invalid_request_error: Invalid request data` before any tool call was attempted; re-running the identical ticket text one line differently (`python -c` calling `handle_ticket()` directly) succeeded cleanly on the first try with no code change in between -- logged as a one-off API-side failure, not a bug in this repo, since it wasn't reproducible. 7/10 cases (EVAL-01/02/03/04/05/06/10) still have `expected: null` and are shown to the user next for confirmation, per their request to see the full input text of the 7 standard cases. Continuation of session `CC-20260831-q7f2`. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched, so no `tsc --noEmit` gate applies to this plain-JSON data file.

## MCP Server & Ticket Queue

- [x] Add `src/ticketQueue.js` — per-ticket JSON queue persistence (add/list/remove)
  - Date: 2026-08-31
  - Session: CC-20260831-n5r8
  - What changed: Added `src/ticketQueue.js`, following `src/saveSupportSummary.js`'s conventions exactly (sync `fs`, fail-closed on bad input, never throws, ES module exports). Exports three functions: `addTicketToQueue(ticket, options?)` — writes one JSON file per ticket to `queue/<requestId>.json` (upsert — calling it twice for the same `requestId` overwrites, never duplicates), reusing `saveSupportSummary.js`'s exact `SAFE_TICKET_ID` filename-safety regex (`/^[A-Za-z0-9_-]+$/`) against `ticket.requestId` rather than re-deriving a new pattern; `listQueuedTickets(options?)` — reads every `.json` file in `queue/`, parses them, and returns them sorted by priority (urgent > high > medium > low, ranked via `classify.js`'s existing exported `PRIORITIES` array rather than a new hardcoded list) with ties broken by earliest `createdAt`; returns a plain empty array when the directory is missing or empty, and a corrupt or wrong-shaped file is skipped (not thrown on) and recorded on the returned array's `.skipped` property instead of crashing the listing; `removeTicketFromQueue(ticketId, options?)` — deletes `queue/<ticketId>.json`, returning `{ ok: false }` (never throwing) for an unsafe/invalid id or a ticket that doesn't exist. Every branch of `addTicketToQueue`/`removeTicketFromQueue` builds a structured `logEntry` (Observability Framework shape: timestamp/level/service/event/outcome/context), matching every other module in `src/` — not persisted here, persistence is the caller-side `auditedActions.js` pattern already established by STORY-003. Added `tests/ticketQueue.test.js` matching `tests/saveSupportSummary.test.js`'s test style (temp-dir-per-test via `randomUUID()`, `captureStderr` helper for the write-blocked case).
  - Verification: `npm test` — 202/202 pass (23 new tests in `tests/ticketQueue.test.js`: happy path for all three functions, directory auto-creation, overwrite/idempotency on repeat add, priority sort with a `createdAt` tie-break, empty-queue-directory and missing-queue-directory both returning `[]`, a corrupt/non-JSON file and a valid-JSON-but-wrong-shape file both skipped and recorded in `.skipped` rather than crashing the listing, non-`.json` files ignored, unsafe/missing ticket id rejected without touching the filesystem, remove-of-nonexistent-ticket and remove-twice both failing closed rather than throwing); the other 179 pre-existing tests unaffected. `git status --porcelain` confirmed only the two new files were added — no existing file was modified.
  - Notes: This is a catch-up entry — this conversation opened directly on a documentation-search request and did not run the Session Start Protocol before this implementation work began; session ID `CC-20260831-n5r8` minted now per the Catch-up rule, distinct from today's other two in-use IDs (`CC-20260831-q7f2`, `CC-20260831-ytlb`) so this conversation's entries aren't mis-attributed during an end-of-session audit. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched (this repo's actual build is the standalone `src/` prototype tree, not the Colaberry monorepo layers described in the template CLAUDE.md — see this file's opening note), so no `tsc --noEmit` gate applies to this plain JS file.

- [x] Add `pending-review-requests` MCP resource and `submitReviewDecision` MCP tool to `mcp-server.js`
  - Date: 2026-08-31
  - Session: CC-20260831-n5r8
  - What changed: Extended the existing `mcp-server.js` (previously exposing only `classify`/`knowledgeBaseSearch`/`generateDraftResponse` as tools) without removing or changing any of its existing imports, tools, or handler branches. Added `resources: {}` to the `Server`'s declared capabilities alongside the existing `tools: {}`, and registered one resource, `pending-review-requests` (`uri: "review://pending-requests"`, `mimeType: "application/json"`): a `ListResourcesRequestSchema` handler advertises it, and a `ReadResourceRequestSchema` handler calls `listQueuedTickets()` and returns `{ result: <tickets>, skipped: <tickets.skipped> }` as formatted JSON text — deliberately rebuilt as a plain object rather than returning the array as-is, since `JSON.stringify` silently drops a non-index property like `.skipped` from an array, which would have hidden exactly the corrupt-file information the resource is supposed to surface. Added a new `submitReviewDecision` tool (`requestId: string` required; `decision: { action: "approve"|"reject", reviewer: string, reason?: string }` required) to the existing `ListToolsRequestSchema` tools array, and a new branch in `CallToolRequestSchema`: looks up the ticket in `listQueuedTickets()` by `requestId` (returns a structured `{ ok: false, error }` text response, never throws, if not found); on `"approve"`, builds a workflow bundle (`{ ticketId: ticket.requestId, ...ticket }` — bridges the queue's `requestId` key to `generateSupportSummary.js`'s required `workflow.ticketId` field without modifying either module), calls `generateSupportSummaryAndLog()` then `saveSupportSummaryAndLog()` then `removeTicketFromQueue()` in sequence, only removing the ticket after a successful save, and returns the summary result; on `"reject"` (as first built, later reworked — see the next entry), called `removeTicketFromQueue()` directly. The whole branch is wrapped in try/catch returning a structured error object, so no unhandled exception can escape the tool call.
  - Verification: `node --check mcp-server.js` — syntax OK. `git status --porcelain -- mcp-server.js src/ticketQueue.js src/auditedActions.js` confirmed only `mcp-server.js` was modified; `src/ticketQueue.js`/`src/auditedActions.js` untouched, used only via their existing exports. Re-ran `npm test` — 202/202 pass, unaffected (no test file exists for `mcp-server.js`, consistent with this repo's existing convention of not unit-testing the three pre-existing tool branches either — it is a thin stdio-transport wrapper around already-tested `src/` functions).
  - Notes: Continuation of session `CC-20260831-n5r8`. `addTicketToQueue` was imported per explicit instruction but is not called anywhere in this branch — nothing in the request has the MCP server itself enqueue tickets, so it is currently unused here (available for a future tool that does). Assumption logged: a queued ticket is expected to already carry the full workflow bundle (`requestText`/`classification`/`classificationReview`/`draftResponse`/etc.) as extra fields beyond the minimal `{requestId, priority, createdAt}` shape exercised in `tests/ticketQueue.test.js` — nothing in this repo yet calls `addTicketToQueue` with a full bundle, so this is unverified against a real end-to-end run. No `tsc --noEmit` gate applies (plain JS, no `/backend`/`/frontend` paths touched).

- [x] Rework `submitReviewDecision`'s reject branch: log to the audit trail and requeue with rejection metadata instead of deleting the ticket
  - Date: 2026-08-31
  - Session: CC-20260831-n5r8
  - What changed: Changed only the `decision.action === "reject"` branch added in the previous entry; the approve branch is untouched. Instead of calling `removeTicketFromQueue()`, the branch now: (1) builds a structured `logEntry` matching the same shape used throughout `src/` (timestamp/level/service: `"mcp-server"`/event: `"ticket_rejected"`/outcome/context: `{ requestId, reviewer, reason }`) and persists it via `appendAuditEntry()` (imported from `src/auditLog.js`, its existing sanctioned write path — not modified); (2) re-adds the same ticket via `addTicketToQueue()` (an upsert, so this overwrites the existing queue file in place rather than needing a separate remove-then-add) with two new fields merged in: `previouslyRejected: true` and `rejectionReason` (the given reason, or `null`); a `rejectionCount` field starts at `1` on a ticket's first rejection and increments from its existing value (`ticket.rejectionCount ?? 1) + 1`) on each subsequent one, keyed off whether `ticket.previouslyRejected` was already `true`; (3) returns a response confirming both the audit-log result and the requeue result, including the ticket's updated `rejectionCount`. `appendAuditEntry()` already never throws (fails closed internally, alerting to stderr on a write failure) so it needed no extra try/catch beyond the branch's existing outer one.
  - Verification: `node --check mcp-server.js` — syntax OK. `git status --porcelain -- mcp-server.js src/ticketQueue.js src/auditLog.js` confirmed only `mcp-server.js` changed; `src/ticketQueue.js` and `src/auditLog.js` untouched, used only via their existing exported functions (`addTicketToQueue`, `appendAuditEntry`) per instruction. Re-ran `npm test` — 202/202 pass, unaffected.
  - Notes: Continuation of session `CC-20260831-n5r8`. Not exercised against a real live MCP client call in this session (no test file covers `mcp-server.js`, consistent with the prior entry) — verified by static syntax check and code review against the exact instruction only, flagged as the honest scope of verification actually performed. No `tsc --noEmit` gate applies (plain JS, no `/backend`/`/frontend` paths touched).

- [x] Add structured MCP log notifications (correlation IDs, real disk-read/rejection/error boundaries) to `mcp-server.js`
  - Date: 2026-08-31
  - Session: CC-20260831-lg8t
  - What changed: Declared the `logging: {}` capability on the `Server` constructor in `mcp-server.js` (the `@modelcontextprotocol/sdk` `Server` class auto-registers the `logging/setLevel` request handler once this capability is present — confirmed by reading `node_modules/@modelcontextprotocol/sdk/dist/esm/server/index.js`, no handler code needed on this side). Added `src/mcpLogEvents.js`, a new pure module of stably-named event builders (`toolInvocationStarted`, `toolInvocationFinished`, `diskReadStarted`, `diskReadFinished`, `requestRejected`, `toolInvocationError`), each returning `{ level, data }` ready for `server.sendLoggingMessage(...)`; this is deliberately a separate concern from the existing per-module `logEntry` objects (`classify.js` etc.) that feed the persistent hash-chained audit trail via `auditLog.js` (STORY-003) — those include fields like `summary` that must never reach this new real-time channel. Reworked the `CallToolRequestSchema` handler in `mcp-server.js` into an outer wrapper (mints one `randomUUID()` correlation id per tool invocation via `node:crypto`, times the call, emits `tool_invocation_started`/`tool_invocation_finished`, and emits `tool_invocation_error` for any error not already logged as a rejection) around a `dispatchTool()` function holding the four tools' existing logic, instrumented with: `request_rejected` (reason: `unknown_tool`) for an unrecognized tool name and (reason: `ticket_not_found`) when `submitReviewDecision` can't find the ticket; `tool_invocation_error` with the already-existing stable `error_class` values (from each callee's own `logEntry`/`result`) for `classify`/`knowledgeBaseSearch`/`generateDraftResponse` validation failures and for `submitReviewDecision`'s requeue/summary-generate/summary-save failure branches. Added an `onDiskRead` hook (optional 4th/last-options param, no-op by default) to `loadKnowledgeBase()` in `src/knowledgeBaseSearch.js` and `loadTemplates()` in `src/generateDraftResponse.js` — fired exactly around the real `readFile` boundary (not on validation failures upstream, which never reach it) with `{ phase: 'started'|'finished', file, outcome?, durationMs?, errorClass? }`; `mcp-server.js` wires this to `diskReadStarted`/`diskReadFinished` log notifications carrying the correlation id. `emitLog()` wraps every `server.sendLoggingMessage()` call in try/catch so a logging failure (no client attached, closed transport) can never break the tool call it's describing. No log payload anywhere includes `requestText`, `summary`, `decision.reason`, `draftText`, or any other free-text field a user typed — every field is a correlation id, tool/event name, timestamp, outcome, duration, fixed file identifier, or a stable error-class/reason code.
  - Verification: `node --check` passed on all 4 touched/added files. `npm test` — 214/214 pass (was 202 before this session; +12 new: 7 in `tests/mcpLogEvents.test.js` covering every builder's shape/level and a forbidden-free-text-key check, 2 new `onDiskRead` cases each in `tests/knowledgeBaseSearch.test.js` and `tests/generateDraftResponse.test.js` covering the started/finished(success) and finished(failure)-with-no-fire-on-pre-read-validation-failure paths, all existing tests unaffected since `onDiskRead` is optional and unused by them). Manually spawned `mcp-server.js` as a real child process over stdio three times: (1) `initialize` confirmed `capabilities.logging: {}` is actually advertised in the handshake response; (2) a `classify` call showed exactly `tool_invocation_started` → `tool_invocation_finished(success, durationMs)` on the `notifications/message` channel, with the tool's own JSON result (which legitimately echoes the typed request text back, as designed) staying separate from the clean log payloads; (3) concurrent `knowledgeBaseSearch` / `submitReviewDecision` (bad requestId) / unknown-tool calls in one batch showed `disk_read_started`→`disk_read_finished(success, file: "knowledgeBase.json")`, `request_rejected(reason: "ticket_not_found")`, and `request_rejected(reason: "unknown_tool")` respectively, each correctly correlationId-scoped to its own invocation and the unknown-tool case emitting exactly one rejection log (not also a duplicate generic error log, confirming the `mcpLogRejectionLogged` dedup flag works).
  - Notes: Session `CC-20260831-lg8t` ran the Session Start Protocol's spirit (read `CLAUDE.md` and `PROGRESS.md`, confirmed no code changes before this work) but did not mint a fresh ID until reaching this write step; distinct from today's other in-use IDs (`CC-20260831-q7f2`, `CC-20260831-ytlb`, `CC-20260831-n5r8`) so this conversation's entries aren't mis-attributed. Deliberately out of scope, per the task's own framing ("this project has no network/external calls — the closest boundary is reading knowledgeBase.json or responseTemplates.json"): disk *writes* (audit log append, ticket queue add/remove, summary save) are not instrumented with the new MCP log channel — those already have their own fail-loud `ALERT:`-to-stderr path in `auditLog.js`/`ticketQueue.js`/`saveSupportSummary.js`, and adding a second logging path for them was not asked for. Also out of scope: the `ListResourcesRequestSchema`/`ReadResourceRequestSchema` handlers (the `pending-review-requests` resource) — the task scoped correlation ids to "per tool invocation," and resource reads are a different request type with no equivalent concept defined; flagged here rather than silently expanded. Log event vocabulary for review (6 stable names): `tool_invocation_started`, `tool_invocation_finished`, `disk_read_started`, `disk_read_finished`, `request_rejected`, `tool_invocation_error`. No `tsc --noEmit` gate applies (plain JS, no `/backend`/`/frontend` paths touched).

- [x] Resolve EVAL-08's `confidence` discrepancy in `assistant/eval_set.json` -- user corrected `low` to `medium`
  - Date: 2026-09-01
  - Session: CC-20260901-9h3k
  - What changed: Per explicit user instruction, updated EVAL-08's `expected` block: `category: "login_problem"`, `priority: "medium"`, `confidence: "medium"` (was `null`, and previously `low` per the user's own prior input logged in the 2026-08-31 entry above), `escalation_recommended: false`. `kb_articles_cited` left `null` -- not part of this request, and still unresolved per the user's own stated preference ("whatever the real KB search actually returns") from the prior session. Extended the case's `note` field to record that the user-confirmed `category` (`login_problem`) still diverges from the live pipeline's observed classification (`access_permission_issue`) documented in the 2026-08-31 entry, and that `confidence: medium` is now consistent with the direct `search_knowledge_base("login_problem")` finding from that same entry (category-only matches on this KB score `medium`, never `low`) -- so this edit resolves the confidence half of the flagged conflict but the category half remains a deliberate user judgment call (expected/ground-truth values in an eval are not required to match what the current implementation produces) rather than a resolved discrepancy.
  - Verification: `python -c "import json; json.load(open('assistant/eval_set.json'))"` -- valid JSON, confirmed. No pipeline/grading run performed (this is a data-only edit to expected values, not a code change; `evals.py` was not invoked).
  - Notes: 7/10 cases (EVAL-01/02/03/04/05/06/10) still have `expected: null`. Full input text of the 7 standard `eval_cases.jsonl` cases was re-shown to the user in this same turn per their request, for confirmation. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths touched, so no `tsc --noEmit` gate applies to this plain-JSON data file.

- [x] Correct EVAL-08's `category` in `assistant/eval_set.json` from `login_problem` to `access_permission_issue`
  - Date: 2026-09-01
  - Session: CC-20260901-9h3k
  - What changed: Per explicit user instruction, changed EVAL-08's `expected.category` from `login_problem` to `access_permission_issue`, keeping `priority: "medium"`, `confidence: "medium"`, `escalation_recommended: false`, `kb_articles_cited: null` unchanged from the prior entry. This supersedes the `login_problem` divergence flagged in both the 2026-08-31 and immediately-preceding 2026-09-01 entries: `access_permission_issue` is exactly what the live pipeline classified this ticket as in the 2026-08-31 live run, so EVAL-08 now has no outstanding conflict between user-stated expected values and observed pipeline behavior. Updated the case's `note` field to record the correction and remove the now-stale discrepancy language.
  - Verification: `python -c "import json; json.load(open('assistant/eval_set.json'))"` -- valid JSON, confirmed. No pipeline/grading run performed (data-only edit to expected values).
  - Notes: EVAL-08 is now the 3rd of 3 awkward cases fully confirmed (alongside EVAL-07, EVAL-09 from 2026-08-31). Full input text of the 7 standard `eval_set.json` cases (EVAL-01/02/03/04/05/06/10, all still `expected: null`) was shown to the user in this same turn per their request, for confirmation next. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths touched, so no `tsc --noEmit` gate applies to this plain-JSON data file.

- [x] Lock in `expected` values for the final 7 standard `assistant/eval_set.json` cases (EVAL-01/02/03/04/05/06/10) -- eval set is now fully confirmed
  - Date: 2026-09-01
  - Session: CC-20260901-9h3k
  - What changed: Ran EVAL-01, 02, 03, 05, 06, 10 live through `handle_ticket()` (direct import, not the CLI wrapper) via a throwaway script in the session scratchpad, one call per ticket, each wrapped in a retry-once-on-exception loop; EVAL-04 was not re-run since its 2026-08-31 observed result was reused verbatim per user instruction. EVAL-01's first attempt hit a transient `JSONDecodeError` on the model's raw response text (unterminated string) -- same one-off, non-reproducible class of API hiccup already logged for EVAL-08 in the 2026-08-31 entry, not a code bug; the retry succeeded cleanly. User reviewed all 6 live results plus the reused EVAL-04 result and confirmed all 7 as-is with no corrections. Wrote each into its case's `expected` block in `assistant/eval_set.json`: EVAL-01 (`login_problem`/`high`/`high`/`false`/`[KB-001, KB-002]`), EVAL-02 (`access_permission_issue`/`medium`/`high`/`true`/`[KB-003]`), EVAL-03 (`power_bi_report_issue`/`high`/`high`/`true`/`[KB-004]`), EVAL-04 (`sql_database_issue`/`medium`/`high`/`false`/`[KB-005]`), EVAL-05 (`data_issue`/`medium`/`high`/`false`/`[KB-006]`), EVAL-06 (`technical_question`/`low`/`low`/`true`/`[KB-007]`), EVAL-10 (`sql_database_issue`/`medium`/`medium`/`false`/`[KB-005]`). Added a provenance `note` to each of the 7 cases recording the live-run source (and EVAL-01's retry). Per user instruction, flagged a known, non-urgent knowledge-base content gap in EVAL-06's `note`: `KB-007` ("General how-to / technical question") in `src/data/knowledgeBase.json` is only a generic escalation-handling stub with no real steps, which is why this SSMS-to-Power-BI-export how-to question came back `confidence: low` / `escalation_recommended: true` instead of a direct answer; a real KB article for data-export/connection how-tos would let the assistant answer such `technical_question`-category tickets directly. Also updated the file's top-level `status` (from `"DRAFT - expected values not yet confirmed"` to `"CONFIRMED - all 10 cases' expected values user-confirmed as of 2026-09-01"`) and `instructions_for_reviewer` fields, since both were now stale once every case had confirmed values -- this was not explicitly requested but is a direct, low-risk accuracy correction to the same file's own metadata, not a scope expansion.
  - Verification: `python -c "import json; json.load(open('assistant/eval_set.json'))"` -- valid JSON, 10 cases. Follow-up script confirmed zero remaining `null` values across all 10 cases' `expected` blocks except EVAL-08's `kb_articles_cited` (intentionally still `null`, per the user's own stated preference from the prior session, not part of this request). No grading run performed yet (`evals.py` was not invoked) -- the eval set is now ready to drive one as a separate next step.
  - Notes: The eval set (`assistant/eval_set.json`) is now fully confirmed across all 10 cases (10/10, up from 3/10 at the start of this session). Natural next step, not yet requested: actually run `assistant/evals.py` against this file to grade `handle_ticket()`'s current behavior against these locked-in expectations. Continuation of session `CC-20260901-9h3k`. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths touched, so no `tsc --noEmit` gate applies to this plain-JSON data file.

- [x] Lock EVAL-08's `kb_articles_cited`; extend `evals.py` to grade `eval_set.json`; fix a real max_tokens truncation bug found while running the grading pass; run the full 10-case grading pass
  - Date: 2026-09-01
  - Session: CC-20260901-9h3k
  - What changed: (1) Ran EVAL-08's ticket live through `handle_ticket()` one more time as requested; it returned `kb_articles_cited: ["KB-003", "KB-004"]`, matching both this run and the 2026-08-31 run, so that was written into `assistant/eval_set.json`'s `expected` block, which now has zero remaining `null` fields across all 10 cases. This same run also returned `escalation_recommended: true`, which conflicts with the case's existing user-set `expected.escalation_recommended: false` from 2026-08-31 -- not part of this request, left as-is but flagged plainly in the case's `note` field as unresolved (second live confirmation of the same conflict, previously only observed once). (2) `assistant/evals.py` was hardcoded to only read `eval_cases.jsonl` (JSONL, 2-field expected). Extended it non-breakingly: added `read_eval_set_cases()` for the JSON/`cases`-array format, and gave `main()` an optional CLI path argument (`.json` suffix routes through the new reader, anything else through the original JSONL reader); with no argument, behavior is unchanged from before (still grades `eval_cases.jsonl`), so the existing invocation's contract is preserved. Also generalized the failure-reporting line in `run_eval()` to report a per-field `(expected, actual)` mismatch dict keyed by the case's `id` (falling back to `#<index>`) instead of hardcoding `category`/`escalation_recommended` field names, since `eval_set.json` has 5 graded fields, not 2. (3) First two grading attempts against `assistant/eval_set.json` crashed the whole run: attempt 1 hit the same `JSONDecodeError: Unterminated string` seen (and previously mis-logged as "a one-off, non-reproducible API glitch") on EVAL-08 (2026-08-31) and EVAL-01 (earlier today) -- three occurrences of the identical failure signature is a real pattern, not a fluke, per this repo's own Stall Detection rule. Root-caused it: `assistant/client.py`'s `DEFAULT_MAX_TOKENS` was `1024`, too tight for `core.py`'s `OUTPUT_SCHEMA` given `draft_reply` alone regularly runs 400-700+ tokens with `escalation_reason`/`confidence` required after it in the schema -- verbose tickets truncate mid-JSON-string. Bumped to `2048`, re-ran, hit a second manifestation of the same root cause: a `StopIteration` in `core.py`'s `text = next(b.text for b in response.content if b.type == "text")`, because a mid-loop tool-use turn (not just the final answer turn) got truncated by `max_tokens`, leaving `response.content` with no text block -- `core.py` had no handling anywhere for `stop_reason == "max_tokens"`, only for the separate `MAX_TOOL_TURNS` loop-count guard. Fixed both: bumped `DEFAULT_MAX_TOKENS` again to `3072` for headroom, and added an `elif response.stop_reason == "max_tokens":` branch in `handle_ticket()` (mirroring the existing `loop_limit_exceeded` fallback shape exactly: `general_support_request`/`medium`/`[]`/escalation_recommended=true/`confidence: none`) so a truncated response now fails safe into an honest low-confidence escalation instead of crashing the caller. (4) With that fix in place, `python -m assistant.evals assistant/eval_set.json` completed cleanly: **5/10 matched (score 0.50)**, no safety violations. Failures: EVAL-01 (`confidence`: expected `high`, got `medium`; `escalation_recommended`: expected `false`, got `true`), EVAL-06 (`confidence`: expected `low`, got `medium`), EVAL-07 (`priority`: expected `low`, got `medium`; `confidence`: expected `none`, got `low`; `kb_articles_cited`: expected `[]`, got `['KB-007']`), EVAL-08 (`escalation_recommended`: expected `false`, got `true` -- the same conflict flagged in this entry's item (1)), EVAL-09 (`priority`: expected `low`, got `medium`; `confidence`: expected `none`, got `low`). Passed: EVAL-02, 03, 04, 05, 10.
  - Verification: `python -c "import ast; ast.parse(open(...).read())"` -- syntax OK on all three touched files (`client.py`, `core.py`, `evals.py`). `python -c "import json; json.load(open('assistant/eval_set.json'))"` -- valid JSON after the EVAL-08 edit. Full grading run: `python -m assistant.evals assistant/eval_set.json`, exit code 0, live against the real API, full output captured to the session scratchpad log -- `Score: 0.50 (5/10 cases matched)`, no `SAFETY VIOLATION` entries (i.e. `requires_human_approval`/`status`/forbidden-phrase checks all held on every case, including the 5 that mismatched on graded fields). Confirmed the no-argument invocation's contract is unchanged by reading the new `main()` branch (falls through to the original `EVAL_CASES_PATH`/`read_eval_cases` path when `sys.argv` has no extra argument) -- not re-run against `eval_cases.jsonl` live in this session (no code path affecting it changed beyond the untouched default branch), so this is a code-review-level confirmation, not a live-run one.
  - Notes: **Correction to the 2026-08-31 and earlier-today entries**: the "transient, one-off, non-reproducible API hiccup" characterization of the `JSONDecodeError` was wrong -- it was a real, reproducible token-budget bug, now fixed at the root (max_tokens bump + explicit `stop_reason == "max_tokens"` handling) rather than papered over with retries. The 5 grading mismatches are genuine, now-real signal about `handle_ticket()`'s current behavior vs. user-set ground truth (not artifacts of the crash) -- EVAL-01/06/07/08/09 all trend toward the live model being *less confident* and, on EVAL-01/08, *more escalation-happy* than the user's locked-in `expected` values call for; not fixed or investigated further here, as fixing `handle_ticket()`'s behavior to chase these scores was not requested -- only running the grading pass was. Continuation of session `CC-20260901-9h3k`. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths touched (all changes are inside the standalone `assistant/` Python package), so no `tsc --noEmit` gate applies; no unit test exists yet for `handle_ticket()`'s new `max_tokens` fallback branch, consistent with this package's established pattern of live-run verification over mocked unit tests (see the 2026-08-31 `assistant/assistant.py` entry) -- flagged rather than silently skipped, since this repo's Testing rules do call for at least a happy-path test on new business logic in the target folders, which this package sits outside of.

- [x] Investigate EVAL-01/EVAL-08 grading mismatches; user determined both are intentional human-in-the-loop behavior, not bugs -- corrected `expected` accordingly (**REQ-006 escalation-bias finding**)
  - Date: 2026-09-01
  - Session: CC-20260901-9h3k
  - What changed: At the user's request, traced the raw model reasoning behind the EVAL-01 and EVAL-08 grading mismatches from the prior entry's run (no code changed for this step, investigation only): **EVAL-01** — the model's `escalation_reason` showed it hedged because the ticket describes a compound symptom (repeated session drops, then a full lockout with a hung login screen) that doesn't cleanly match either retrieved KB-001 (lockout/reset) or KB-002 (MFA) individually, even though both matched the `login_problem` category well. **EVAL-08** — the model's `escalation_reason` cited two things: genuine category ambiguity (the case's own designed `awkward_type: "genuinely_ambiguous"`), and, more structurally, that KB-003 (the `access_permission_issue` article) has its own documented step reading "this requires a permission change — the assistant may only recommend it; a human must approve and apply it" — meaning that category escalates almost by construction, independent of ambiguity. Traced the shared mechanism to `assistant/prompt.py`'s `SYSTEM_PROMPT`: its escalation clause ("If the knowledge base has no good match, **or the issue is outside what a documented procedure can resolve**, recommend escalation") is deliberately unanchored to the KB tool's own numeric/label match confidence, and REQ-006 (the requirement this escalation behavior traces back to, per `PROGRESS.md`'s 2026-08-23 STORY-006 entry and `src/generateEscalationRecommendation.js`) is written the same way — favor a human in the loop under genuine ambiguity rather than force a confident automated answer. Presented this analysis to the user with reasoning quoted verbatim; **the user reviewed it and determined this is correct, designed behavior, not a defect** — it matches this project's human-in-the-loop principle. Per that explicit determination, corrected `assistant/eval_set.json`'s `expected` blocks: EVAL-01's `confidence` (`high` → `medium`) and `escalation_recommended` (`false` → `true`), keeping `category`/`priority`/`kb_articles_cited` unchanged; EVAL-08's `escalation_recommended` (`false` → `true`), keeping `category`/`priority`/`confidence`/`kb_articles_cited` unchanged. Extended both cases' `note` fields and the file's top-level `instructions_for_reviewer` with this finding and a pointer back to this entry, per the user's instruction to document it "wherever fits best." **EVAL-06, EVAL-07, and EVAL-09's mismatches were deliberately left untouched** — the user wants to look at those separately; they remain open, unresolved grading failures.
  - Verification: `python -c "import json; json.load(open('assistant/eval_set.json'))"` -- valid JSON, 10 cases, confirmed `EVAL-01.expected == {category: login_problem, priority: high, confidence: medium, escalation_recommended: true, kb_articles_cited: [KB-001, KB-002]}` and `EVAL-08.expected == {category: access_permission_issue, priority: medium, confidence: medium, escalation_recommended: true, kb_articles_cited: [KB-003, KB-004]}`. No code changed and no new grading run performed in this step (data-only `expected`/`note` edits, informed by the already-captured raw reasoning from the prior entry's grading run and one earlier live EVAL-08 call this session) — re-running `evals.py` to confirm these two now score as pass/fail against updated `expected` is a natural next step, not yet requested.
  - Notes: This means the eval set's ground truth is no longer purely "what the user independently decided is correct" for these two cases — it's now "what the live pipeline actually and consistently does, endorsed by the user as correct after seeing why." That's a meaningfully different kind of `expected` value than the rest of the file (most cases were locked in from a single observed run without this level of scrutiny), worth keeping in mind if `handle_ticket()`'s prompt or KB content changes later and these two cases start failing again — the right response then is to re-examine, not assume regression. Continuation of session `CC-20260901-9h3k`. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths touched, so no `tsc --noEmit` gate applies to this plain-JSON data file.

- [x] Fix EVAL-07's root cause: stop `handle_ticket()` from searching a second, unrelated category — real code fix, not an `expected`-value edit (**category-lock fix**)
  - Date: 2026-09-01
  - Session: CC-20260901-9h3k
  - What changed: Per the prior entry's traced-but-unfixed EVAL-07 finding (the assistant made a speculative second `search_knowledge_base` call under `technical_question` after its correctly-scoped `general_support_request` search came back weak, and `KB-007` false-matched purely on `tools.py`'s flat `+3` category-match bonus with no relevance floor), implemented the fix at two levels per this repo's "LLMs are probabilistic, production systems must be deterministic" principle — prompt guidance alone isn't a guarantee. (1) **Deterministic guardrail** in `assistant/core.py`'s `handle_ticket()`: added a `searched_category` tracker; any `search_knowledge_base` call after the first is now checked against it, and a call requesting a *different* category is intercepted before it reaches `tools.search_knowledge_base()` — instead of a real KB lookup, the model receives a synthetic rejection result (`found: false`, explanatory `message`) so it self-corrects within the same tool-use turn rather than the call silently failing or crashing. (2) **Prompt reinforcement** in `assistant/prompt.py`'s `SYSTEM_PROMPT` point 3: added an explicit instruction to search only the single classified category and treat a bad match there as itself the answer (recommend escalation) rather than trying another category. (3) **Verified EVAL-07 live**: exactly one search call now (`general_support_request` only), `confidence: none` and `kb_articles_cited: []` — both now match `expected` exactly (no edit needed to those fields); `priority` (expected `low`, observed `medium`) remains the same pre-existing, unrelated mismatch flagged before this fix, left open. (4) **Re-ran the full 10-case grading suite** to check for regressions, since this fix changes `handle_ticket()` globally, not just EVAL-07's ticket — three other cases (EVAL-02, EVAL-03, EVAL-08, EVAL-10) had previously made a second, different-category search too. Result: every one of the 10 cases made exactly one search call this run (grep for `Rejected:` in the run log found zero hits — the prompt-level instruction alone was sufficient this run; the code-level guardrail never had to fire as a backstop, so its actual interception path is unverified by a live run, only by reading the code). Score held at 5/10, but the *composition* of failures changed: EVAL-08 (deliberately `awkward_type: "genuinely_ambiguous"` between `access_permission_issue` and `power_bi_report_issue`) now only searches its first-classified category and cites `kb_articles_cited: ["KB-003"]` instead of the previously-locked `["KB-003", "KB-004"]` — a real, directly-traced side effect: the fix that correctly blocks a low-value fallback search on EVAL-07 also blocks a plausibly-legitimate one on a case built specifically to span two categories. EVAL-03 and EVAL-10 also newly mismatched (both had passed before this fix) and both also dropped from two searches to one in this run — flagged to the user as a possible but *unconfirmed* second-order effect of the fix, since ordinary model run-to-run variance (already directly observed multiple times this session, e.g. EVAL-01's two different outcomes on an identical unchanged ticket) can't be ruled out without further repeated runs. Presented all of this to the user before editing any `expected` values, via `AskUserQuestion`: asked how to handle EVAL-08 (accept new behavior / leave open / investigate a targeted exception) and whether to investigate the EVAL-03/EVAL-10 flip now. **User chose "leave it open" for both** — EVAL-08's `expected` was deliberately left at `["KB-003", "KB-004"]` (not updated to match new behavior), and EVAL-03/EVAL-10 were left uninvestigated. Documented all of this — the fix, EVAL-07's resolution, and the EVAL-03/EVAL-08/EVAL-10 side effects — directly in each affected case's `note` field in `assistant/eval_set.json`, plus the file's top-level `instructions_for_reviewer`, which now lists EVAL-03/06/07(priority only)/08/09/10 as the full set of currently-open, untouched mismatches.
  - Verification: `python -c "import ast; ast.parse(...)"` — syntax OK on `core.py` and `prompt.py`. Standalone live re-run of EVAL-07's exact ticket through `handle_ticket()` (direct import) — one search call, `confidence: none`, `kb_articles_cited: []`, matches `expected`. Full-suite live re-run: `python -m assistant.evals assistant/eval_set.json`, exit code 0, `Score: 0.50 (5/10 cases matched)`, no `SAFETY VIOLATION` entries, full output captured to the session scratchpad log; cross-checked every case's tool-call category arguments in that log (`grep "Tool arguments: {'category'"`) — confirmed exactly 10 search calls total across 10 cases (one each), and zero `Rejected:` messages, confirming the guardrail's non-firing was because the prompt alone worked this run, not because the guardrail was bypassed. `python -c "import json; json.load(...)"` — valid JSON on `eval_set.json` after all `note`/`instructions_for_reviewer` edits.
  - Notes: The guardrail's actual interception branch (the synthetic `found: false` rejection result) has not yet been exercised by any live run in this session — it's currently a defense-in-depth backstop verified only by code review, not by observing it actually fire against a real model attempt. If the model's behavior drifts back toward speculative re-searching in a future session (prompt changes, model version changes, etc.), that branch is expected to catch it, but this hasn't been proven live — worth a deliberate test if this ever matters more than opportunistically-observed reliance. Also worth flagging: this fix embeds an assumption — that a ticket belongs to exactly one searchable category — which is in some tension with `OUTPUT_SCHEMA`'s own "classify into exactly one category" constraint being asked to also serve genuinely cross-category tickets like EVAL-08; the fix didn't create that tension (EVAL-08's dual-category behavior was already somewhat at odds with the single-classification schema), it just made it visible and enforced. Continuation of session `CC-20260901-9h3k`. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths touched (changes confined to the standalone `assistant/` Python package), so no `tsc --noEmit` gate applies; no unit test exists yet for the new `searched_category` rejection branch, consistent with this package's established live-run-over-mocked-test pattern (flagged, not silently skipped, same as the earlier `max_tokens` fallback branch).

- [x] Deliberate-failure exercise: copy `assistant/` to `assistant_broken/`, inject 3 faults, run both against `eval_set.json` for a real score comparison (no fix applied yet)
  - Date: 2026-09-01
  - Session: CC-20260901-v4qz
  - What changed: Per explicit user request ("do NOT change my working files"), copied all 9 source files from `assistant/` to a new sibling `assistant_broken/` (excluding `__pycache__`), leaving `assistant/` byte-for-byte untouched (confirmed via `grep MAX_TOOL_TURNS assistant/core.py` still showing the original guard after the copy). Injected exactly three faults in `assistant_broken/` only, each marked with a `FAULT INJECTED (deliberate failure exercise)` comment: (1) `client.py` — replaced the `anthropic.Anthropic()` env-var lookup with a hardcoded fake placeholder key (`sk-ant-PLACEHOLDER-BROKEN-DO-NOT-USE-...`), a deliberate Secrets Management violation for the exercise. (2) `core.py` — removed the `MAX_TOOL_TURNS = 5` constant and its `if tool_turns > MAX_TOOL_TURNS: break` guard entirely (plus the now-dead `loop_limit_exceeded` branch and `tool_turns` counter), so the tool-use `while` loop has no turn cap. (3) `prompt.py` — rewrote `SYSTEM_PROMPT` from the specific, numbered, five-step original into a short, vague, non-directive paragraph ("Try to figure out what kind of ticket this is... use your judgment... maybe it should go to a person instead").
  - Verification: Ran both packages live against the real API (`ANTHROPIC_API_KEY` confirmed loaded via `find_dotenv()`/`load_dotenv()`, length 108) using the fully-confirmed 10-case `eval_set.json` (copied verbatim into `assistant_broken/`, no `expected` values touched). **Original** (`python -m assistant.evals assistant/eval_set.json`): unchanged from the prior session's result, `Score: 0.50 (5/10 cases matched)`, no safety violations. **Broken** (`python -m assistant_broken.evals assistant_broken/eval_set.json`): every one of the 10 cases fails immediately with `anthropic.AuthenticationError: Error code: 401 - API key is invalid` — the pipeline never reaches the model at all, so broken-copy score is effectively **0/10 (0.00)**. This is fault (1) alone dominating: the hardcoded invalid key blocks every request before faults (2) (unbounded tool loop) or (3) (vague prompt) ever get a chance to be exercised, so this run does not by itself demonstrate what (2)/(3) alone would do to the score — only that a hardcoded bad key is a total, immediate failure. `python -c "import ast; ast.parse(...)"` confirmed syntax OK on all three edited `assistant_broken/` files before running.
  - Notes: Per explicit instruction, nothing was fixed — `assistant_broken/` is left in its broken state for a future exercise step. If a score for faults (2)/(3) in isolation (i.e., unbounded loop + vague prompt, but with a working key) is wanted, that requires a follow-up run the user hasn't requested yet. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths touched, so no `tsc --noEmit` gate applies; this is a standalone Python exercise copy, consistent with the sibling `assistant/` package's own established live-run-over-mocked-test verification pattern.

- [x] Isolate faults #2 (unbounded tool loop) and #3 (vague prompt) from fault #1 (bad key) — fresh copy with a real key, real eval run
  - Date: 2026-09-01
  - Session: CC-20260901-v4qz
  - What changed: Per explicit follow-up request, made a third fresh copy, `assistant_broken_23/` (all 9 source files from `assistant/`, excluding `__pycache__`), so faults #2/#3 could be scored with a working key rather than restoring the key inside the already-published `assistant_broken/` (kept as-is, untouched, per the prior entry's "leave it broken" instruction). `client.py` was left byte-identical to `assistant/client.py` (confirmed via `diff`) — real env-var key lookup, no fault #1. Applied only fault #2 (`core.py`: removed `MAX_TOOL_TURNS`/its break guard/`loop_limit_exceeded`, same as `assistant_broken/`) and fault #3 (`prompt.py`: same vague rewrite as `assistant_broken/`). One small addition beyond the `assistant_broken/` version, scoped to this diagnostic copy only: kept a `tool_turns` counter (increment-only, no longer enforced as a cap) and added it to `handle_ticket()`'s returned dict, purely so the isolation run could show how many turns the now-uncapped loop actually took per case — `evals.py`'s `case_passes()` only iterates `expected.items()`, so this extra key can't affect grading (same reasoning already documented for the pre-existing `tool_calls` field).
  - Verification: `python -c "import ast; ast.parse(...)"` — syntax OK on `core.py` and `prompt.py`. Ran `python -m assistant_broken_23.evals assistant_broken_23/eval_set.json` live against the real API (full output captured to the session scratchpad log, 540 lines), exit code 0. **Result: Score 0.40 (4/10 cases matched)** vs. the original's 0.50 (5/10) — a real, measured drop attributable to fault #3, since fault #2 never actually fired: every case's diagnostic `tool_turns` was 1 or 2 (max 2, min 1, across all 10 cases), well inside where the original `MAX_TOOL_TURNS = 5` cap would have allowed anyway — so in this run, removing the cap had zero observable effect; it's a latent risk (no ceiling if the model ever did misbehave) rather than a demonstrated failure. No safety violations (`requires_human_approval`/`status`/forbidden-phrase checks all held on all 10 cases, since those fields are Python-hardcoded post-parse and not prompt-dependent). Failed cases and their exact mismatches: EVAL-01 (`confidence`: expected `medium`, got `low`), EVAL-04 (`confidence`: expected `high`, got `medium`), EVAL-06 (`confidence`: expected `low`, got `medium`), EVAL-07 (`priority`: expected `low`, got `medium`; `confidence`: expected `none`, got `low`), EVAL-09 (`priority`: expected `low`, got `medium`; `confidence`: expected `none`, got `low`), EVAL-10 (`priority`: expected `medium`, got `low`). Notably every single mismatch is in `confidence` or `priority` — the two free-judgment fields — with zero mismatches in `category`, `escalation_recommended`, or `kb_articles_cited`, the fields anchored by the KB tool's own results; consistent with the vague prompt removing calibration guidance for subjective judgment calls while leaving tool-grounded decisions comparatively stable. Aggregate token cost for the 10-case run: 40,646 input / 11,162 output tokens (not directly compared to a saved original-run total, since that run's per-case log wasn't captured to a file in the prior entry — only its final tail was shown inline).
  - Notes: This isolates the two faults cleanly but doesn't prove fault #2 is harmless in general — it only shows this particular 10-case eval set's tickets never gave the model a reason to loop past 2 turns, so the missing cap was never tested under stress (e.g., a ticket engineered to make the model keep re-querying, or a genuine model confusion loop). Per instruction, nothing was fixed in any of the three `assistant_broken*` copies — this is diagnostic only. Three sibling copies now exist for this exercise: `assistant_broken/` (all 3 faults, unrunnable due to fault #1), `assistant_broken_23/` (faults #2+#3 only, real key, scored above), and the untouched `assistant/` original (0.50 baseline). No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths touched, so no `tsc --noEmit` gate applies.

- [x] Fix all three injected faults in `assistant_broken/` (hardened, not just reverted) and re-verify the score
  - Date: 2026-09-01
  - Session: CC-20260901-v4qz
  - What changed: Per explicit instruction, fixed all three faults in `assistant_broken/` (harder than plain revert on two of the three) — full diffs against `assistant/` shown to the user inline. (1) `client.py`: removed the hardcoded placeholder key, restored `anthropic.Anthropic()`'s env-var lookup, and added a startup check — `if not os.environ.get("ANTHROPIC_API_KEY"): raise MissingAPIKeyError(...)` (new named exception, not a bare `Exception`/`RuntimeError` string) — so a missing key now fails at import time with a clear message instead of surfacing as an opaque 401 mid-run. Also added `REQUEST_TIMEOUT_SECONDS = 60.0`. (2) `core.py`: restored `MAX_TOOL_TURNS = 5` and the per-turn cap check, but changed its failure mode per explicit instruction — instead of the original's silent graceful-degrade (set a flag, `break`, fall through to a fabricated escalation proposal), it now raises a new named `ToolLoopLimitExceeded(RuntimeError)` when the cap is hit. This is a deliberate behavior change beyond the original `assistant/core.py`, not a pure revert: the original swallowed the loop-limit condition into a normal-looking proposal dict; the fixed version surfaces it as an actual exception, consistent with the user's "not a generic Exception" instruction and this repo's Failure-First Design principle against catching/converting failures into look-normal results. Also added `timeout=client_module.REQUEST_TIMEOUT_SECONDS` to both `.create()` calls (confirmed via `inspect.signature` that the installed `anthropic` 0.120.2 SDK's `messages.create()` accepts a `timeout: float` kwarg directly). (3) `prompt.py`: reverted `SYSTEM_PROMPT` to the original numbered five-step version verbatim — `diff assistant/prompt.py assistant_broken/prompt.py` now produces zero output.
  - Verification: `python -c "import ast; ast.parse(...)"` — syntax OK on all three files. Diffed all three fixed files against `assistant/`'s originals and showed the diffs to the user (client.py and core.py show the intentional hardening deltas beyond a pure revert; prompt.py diffs to nothing). Re-ran `python -m assistant_broken.evals assistant_broken/eval_set.json` live against the real API: exit code 0, **Score: 0.60 (6/10 cases matched)** — at/above the original `assistant/` baseline of 0.50, and well above the 0.40 scored with faults #2+#3 alone in the prior entry's isolation test; the modest 0.50→0.60 delta versus the untouched original is consistent with this eval's already-documented run-to-run variance (see the 2026-09-01 "REQ-006 escalation-bias finding" and "category-lock fix" entries above, both of which independently observed the same tickets scoring differently across repeated live runs), not attributed to the fixes themselves beyond "faults are gone." No safety violations. `ToolLoopLimitExceeded` did not fire in this run (every case's tool loop again stayed well under the cap), so the new raise path is verified only by code review here, not by a live trigger.
  - Notes: Reviewed the codebase for other silent-failure patterns per the user's explicit request (read-only review, nothing changed): (1) `tools.py`'s `search_knowledge_base()` catches `(OSError, json.JSONDecodeError, KeyError)` around the KB file read and converts any of them — file missing, corrupted JSON, unexpected schema — into an ordinary `"found": False` business result with no logging; a human reviewer approving the resulting escalation has no way to tell "KB genuinely has nothing" from "the KB file itself is broken." (2) Same function's keyword-type coercion (`elif not isinstance(keywords, list): keywords = []`) silently drops a malformed tool argument with no log line. (3) `core.py`'s final-answer `else` branch (`next(b.text for b in response.content if b.type == "text")`) assumes every non-`tool_use`, non-`max_tokens` `stop_reason` carries a text block; a stop reason that doesn't (e.g. a safety refusal) raises a bare `StopIteration` with no diagnostic context. (4) The `json.loads(text)` right after it has the same problem in reverse — a schema-violating or malformed model response raises a bare `JSONDecodeError` with no correlation back to which ticket caused it. (5) `evals.py`'s `run_eval()` calls `handle_ticket()` with no exception handling per case, so any single case raising (including the new `ToolLoopLimitExceeded`, or an auth/network error) aborts the whole batch and discards already-graded results instead of recording it as one failed case and continuing. None of these were fixed — reported only, per the user's "list" request, not a "fix" request. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths touched, so no `tsc --noEmit` gate applies.
