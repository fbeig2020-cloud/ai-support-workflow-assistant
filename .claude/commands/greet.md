---
description: Output a friendly greeting message
---

Respond with a short, friendly greeting to the user. If they passed arguments after the command (`$ARGUMENTS`), personalize the greeting using that text (e.g. treat it as a name or context). Otherwise, use a generic warm greeting. Keep it to 1-2 sentences, no preamble.
